---
phase: 04-颜色与高级特性
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/color/ColorPicker.tsx
  - apps/mobile/src/components/color/HexInput.tsx
  - apps/mobile/src/components/canvas/PixelStyleSelector.tsx
  - apps/mobile/src/stores/canvasStore.ts
autonomous: true
requirements: [COLR-03, COLR-04, COLR-05]
user_setup: []
must_haves:
  truths:
    - User can pick custom colors using HSL color picker
    - User can input HEX color codes directly
    - User can select pixel style (circle, square, rounded)
    - Selected color updates immediately in drawing tools
    - Pixel style changes render immediately on canvas
  artifacts:
    - path: "apps/mobile/src/components/color/ColorPicker.tsx"
      provides: "HSL color picker with saturation/lightness sliders"
      min_lines: 100
    - path: "apps/mobile/src/components/color/HexInput.tsx"
      provides: "HEX color input with validation"
      min_lines: 50
    - path: "apps/mobile/src/components/canvas/PixelStyleSelector.tsx"
      provides: "Pixel style toggle (circle/square/rounded)"
      min_lines: 60
    - path: "apps/mobile/src/stores/canvasStore.ts"
      provides: "Pixel style state management"
      exports: ["pixelStyle"]
  key_links:
    - from: "apps/mobile/src/components/color/ColorPicker.tsx"
      to: "canvasStore.selectedColor"
      via: "Update selectedColor on picker change"
      pattern: "canvasStore.setSelectedColor"
    - from: "apps/mobile/src/components/canvas/PixelStyleSelector.tsx"
      to: "canvasStore.pixelStyle"
      via: "Update pixelStyle on selection"
      pattern: "canvasStore.setPixelStyle"
    - from: "apps/mobile/src/components/canvas/CanvasGrid.tsx"
      to: "canvasStore.pixelStyle"
      via: "Render pixels with selected style"
      pattern: "pixelStyle.*CIRCLE.*SQUARE.*ROUNDED"
---

<objective>
Deliver HSL color picker, HEX input, and pixel style selection for custom color and appearance control

Purpose: Users need to pick custom colors beyond the predefined palette and control how pixels render on the canvas. This enables full creative control.

Output: Working color picker with HSL sliders, HEX input validation, and pixel style toggle that integrates with canvas rendering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-高级编辑与用户体验/03-01-SUMMARY.md

# Existing codebase patterns
From @pixelbead/color-system:
```typescript
export function rgbToHsl(r: number, g: number, b: number): { h: number; s: number; l: number }
export function hexToRgb(hex: string): { r: number; g: number; b: number } | null
```

From types.ts:
```typescript
export enum PixelStyle {
  CIRCLE = 'CIRCLE',
  SQUARE = 'SQUARE',
  ROUNDED = 'ROUNDED',
}
```

From canvasStore.ts pattern:
```typescript
selectedColor: ColorHex;
setSelectedColor: (color: ColorHex) => void;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HSL Color Picker component</name>
  <files>
    apps/mobile/src/components/color/ColorPicker.tsx
  </files>
  <action>
    Create apps/mobile/src/components/color/ColorPicker.tsx:

    1. Import: React, View, Slider from 'react-native'
    2. Import: useCanvasStore from '../../stores/canvasStore'
    3. Import: hexToRgb, rgbToHsl from '@pixelbead/color-system'
    4. Component structure:
       - Hue slider (0-360): Full spectrum rainbow gradient background
       - Saturation slider (0-100): Gray to full color gradient
       - Lightness slider (0-100): Black to white gradient
       - Color preview box showing current color
    5. State management:
       - Use useEffect to convert selectedColor to HSL on mount and when color changes from other sources
       - Local state for h, s, l values
       - On slider change: Convert HSL back to RGB to HEX, call canvasStore.setSelectedColor
    6. HSL to RGB conversion helper (use standard algorithm):
       - Convert HSL to RGB values (0-255)
       - Convert RGB to HEX string (#RRGGBB)
    7. Accessibility: Add accessibilityLabel for each slider ("Hue: 180", "Saturation: 50%", "Lightness: 50%")
    8. Styling: Slider height=40px, color preview=80x80px with border

    Design: Vertical layout with three sliders stacked, color preview at bottom. Use NativeWind for styling if available.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    ColorPicker renders HSL sliders, dragging sliders updates color, color preview updates immediately, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HEX Color Input component</name>
  <files>
    apps/mobile/src/components/color/HexInput.tsx
  </files>
  <action>
    Create apps/mobile/src/components/color/HexInput.tsx:

    1. Import: React, View, TextInput, TouchableOpacity, Text from 'react-native'
    2. Import: useCanvasStore, MaterialIcons from '@expo/vector-icons'
    3. Component structure:
       - Text input field for HEX codes (e.g., #FF0000 or FF0000)
       - Validation: Check if input is valid 6-digit HEX (with or without #)
       - Apply button: Disabled when invalid, enabled when valid
       - Copy button: Copy current selectedColor to clipboard
    4. Input validation:
       - Regex: /^#?([0-9A-Fa-f]{6})$/
       - Show error message "Invalid HEX code" if invalid
       - Auto-add # if missing
    5. Apply button action:
       - On press: Validate and call canvasStore.setSelectedColor(validHex)
       - Provide haptic feedback on success (expo-haptics)
    6. Copy button action:
       - Use Clipboard.setStringAsync from expo-clipboard
       - Show toast feedback: "Color copied!"
    7. Accessibility: accessibilityLabel="HEX color input", accessibilityHint="Enter 6-digit HEX code"

    Design: Compact input field (height=44px), inline validation, button right-aligned.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    HexInput accepts HEX codes, validates correctly, applies valid colors, copies to clipboard, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add pixel style state to canvasStore</name>
  <files>
    apps/mobile/src/stores/canvasStore.ts
  </files>
  <action>
    Update apps/mobile/src/stores/canvasStore.ts:

    1. Import PixelStyle enum from @pixelbead/shared-types
    2. Add to CanvasState interface:
       - pixelStyle: PixelStyle (default: PixelStyle.CIRCLE)
       - setPixelStyle: (style: PixelStyle) => void
    3. Add to initial state: pixelStyle: PixelStyle.CIRCLE
    4. Add setPixelStyle action:
       - set({ pixelStyle: style })
    5. Persist pixelStyle in MMKV (already in persist middleware)

    This enables canvas rendering to use different pixel shapes.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    pixelStyle added to canvasStore, setPixelStyle action works, persists to MMKV, TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 4: Build PixelStyleSelector component</name>
  <files>
    apps/mobile/src/components/canvas/PixelStyleSelector.tsx
  </files>
  <action>
    Create apps/mobile/src/components/canvas/PixelStyleSelector.tsx:

    1. Import: React, View, TouchableOpacity from 'react-native'
    2. Import: useCanvasStore, MaterialIcons from '@expo/vector-icons'
    3. Import: PixelStyle enum from '@pixelbead/shared-types'
    4. Component structure:
       - Horizontal row of 3 buttons (Circle, Square, Rounded)
       - Each button: Icon + label, 44x44px tap target
       - Selected style: Blue background, white text
       - Unselected styles: Gray background, black text
    5. Icons:
       - Circle: MaterialIcons.radio-button-checked
       - Square: MaterialIcons.crop-square
       - Rounded: MaterialIcons.check-box-outline-blank (with borderRadius styling)
    6. On press: Call canvasStore.setPixelStyle(style)
    7. Accessibility: accessibilityLabel="Circle pixels", "Square pixels", "Rounded pixels"
    8. Use React.memo for performance

    Design: Compact toolbar button style, matches tool drawer button dimensions.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    PixelStyleSelector renders 3 style options, tapping changes pixelStyle, selected state shows correctly, no TypeScript errors
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **TypeScript compilation**: Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` — must pass with 0 errors
2. **Color picker integration**: Test that changing HSL sliders updates selectedColor in canvasStore
3. **HEX validation**: Test invalid HEX codes (e.g., #ZZZZZZ) are rejected with error message
4. **Pixel style persistence**: Test that pixelStyle persists after app restart (MMKV)
5. **Component rendering**: Verify all three components (ColorPicker, HexInput, PixelStyleSelector) render without crashes
</verification>

<success_criteria>
1. User can pick custom colors using HSL sliders (hue, saturation, lightness)
2. User can input HEX color codes (#RRGGBB format) with validation
3. User can copy selected color to clipboard via HEX input
4. User can switch between pixel styles (circle, square, rounded)
5. Color picker and HEX input both update canvasStore.selectedColor
6. Pixel style changes render immediately on canvas (requires CanvasGrid integration, verified in next phase)
7. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-颜色与高级特性/04-02-SUMMARY.md`
</output>
