---
phase: 04-颜色与高级特性
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/src/components/selection/SelectionOverlay.tsx
  - apps/mobile/src/components/selection/SelectionTools.tsx
  - apps/mobile/src/hooks/useSelection.ts
  - apps/mobile/src/stores/clipboardStore.ts
  - apps/mobile/src/stores/canvasStore.ts
autonomous: true
requirements: [SELC-01, SELC-02, SELC-03, SELC-04, SELC-05, SELC-06]
user_setup: []
must_haves:
  truths:
    - User can select rectangular region on canvas
    - User can copy selected region to clipboard
    - User can paste clipboard content to canvas at selected position
    - User can cut selected region (copy + clear)
    - User can clear selected region
    - User can invert colors in selected region
    - Selection overlay renders on top of canvas
  artifacts:
    - path: "apps/mobile/src/components/selection/SelectionOverlay.tsx"
      provides: "Visual selection rectangle with drag handles"
      min_lines: 120
    - path: "apps/mobile/src/components/selection/SelectionTools.tsx"
      provides: "Selection action toolbar (copy, cut, clear, invert)"
      min_lines: 80
    - path: "apps/mobile/src/hooks/useSelection.ts"
      provides: "Selection logic (drag to select, manipulate region)"
      min_lines: 100
    - path: "apps/mobile/src/stores/clipboardStore.ts"
      provides: "Clipboard state management (selection, paste buffer)"
      min_lines: 60
    - path: "apps/mobile/src/stores/canvasStore.ts"
      provides: "Selection state integration"
      exports: ["selectionRegion", "setSelectionRegion"]
  key_links:
    - from: "apps/mobile/src/hooks/useSelection.ts"
      to: "canvasStore.grid"
      via: "Read/write selected region pixels"
      pattern: "canvasStore.grid.get\\(.*\\)"
    - from: "apps/mobile/src/components/selection/SelectionTools.tsx"
      to: "clipboardStore"
      via: "Copy selection to clipboard, paste from clipboard"
      pattern: "clipboardStore.copy|clipboardStore.paste"
    - from: "apps/mobile/src/stores/clipboardStore.ts"
      to: "canvasStore.grid"
      via: "Write paste buffer to canvas"
      pattern: "canvasStore.setPixel"
---

<objective>
Deliver selection tools with clipboard operations and region manipulation

Purpose: Users need to select, copy, paste, and manipulate regions on the canvas. This enables efficient editing workflows and reusable pixel art components.

Output: Working selection system with rectangular selection, clipboard operations (copy, cut, paste), and region manipulation (clear, invert).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-高级编辑与用户体验/03-01-SUMMARY.md

# Existing codebase patterns
From canvasStore.ts:
```typescript
grid: Map<string, string>; // Key: "x,y", Value: color hex
gridSize: { width: number; height: number };
setPixel: (x, y, color) => void;
```

From Phase 3 drawing tools:
```typescript
// Coordinate transformation: screen to grid
const gridX = Math.floor(screenX / cellSize);
const gridY = Math.floor(screenY / cellSize);
```

From Phase 2 gesture handling:
```typescript
// Gesture.Race pattern for multiple gesture types
<GestureDetector gesture={Gesture.Race(tapGesture, panGesture)}>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clipboard store for selection operations</name>
  <files>
    apps/mobile/src/stores/clipboardStore.ts
  </files>
  <action>
    Create apps/mobile/src/stores/clipboardStore.ts using Zustand pattern (matches canvasStore.ts):

    1. Import: create, persist, createJSONStorage from zustand/middleware, mmkvStorage from '../storage/mmkvStorage'
    2. Define ClipboardState interface:
       - clipboard: Map<string, string> | null (paste buffer, key: "x,y", value: color hex)
       - clipboardOffset: { x: number; y: number } (original offset for positioning)
       - copySelection: (region: { x1, y1, x2, y2 }, grid: Map<string, string>) => void
       - clearClipboard: () => void
    3. Create useClipboardStore:
       - Initial state: clipboard: null, clipboardOffset: { x: 0, y: 0 }
       - copySelection action:
         - Extract pixels from grid within region bounds
         - Store in clipboard Map with normalized coordinates (start at 0,0)
         - Store original offset for paste positioning
       - clearClipboard action: set({ clipboard: null })
    4. Don't persist clipboard to MMKV (transient data)

    Design: Clipboard stores pixel data for pasting anywhere on canvas.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    clipboardStore created, copySelection extracts pixels correctly, clipboard persists in memory (not MMKV), TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Add selection state to canvasStore</name>
  <files>
    apps/mobile/src/stores/canvasStore.ts
  </files>
  <action>
    Update apps/mobile/src/stores/canvasStore.ts:

    1. Add to CanvasState interface:
       - selectionRegion: { x1: number; y1: number; x2: number; y2: number } | null
       - setSelectionRegion: (region) => void
    2. Add to initial state: selectionRegion: null
    3. Add setSelectionRegion action:
       - set({ selectionRegion: region })
    4. Normalize selection coordinates:
       - Ensure x1 <= x2 and y1 <= y2
       - If inverted, swap coordinates
    5. Constrain selection to grid bounds:
       - Clamp coordinates to [0, gridSize.width-1] and [0, gridSize.height-1]

    This enables selection overlay to render and tools to access selected region.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    selectionRegion added to canvasStore, coordinates normalized and constrained, TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 3: Create useSelection hook for selection logic</name>
  <files>
    apps/mobile/src/hooks/useSelection.ts
  </files>
  <action>
    Create apps/mobile/src/hooks/useSelection.ts:

    1. Import: useCanvasStore, useClipboardStore
    2. Export functions:
       - useSelectionGesture(): Returns gesture handler for drag-to-select
       - useSelectionOperations(): Returns { copy, cut, clear, invert } functions
    3. useSelectionGesture implementation:
       - Returns PanGestureHandler using react-native-gesture-handler
       - On gesture start: Record starting point, set selectionRegion to { x1, y1, x1, y1 }
       - On gesture update: Update selectionRegion.x2 and y2 based on pan delta
       - On gesture end: Normalize and constrain selectionRegion
       - Coordinate transformation: Screen coordinates to grid coordinates using cell size
    4. useSelectionOperations implementation:
       - copy(region): Call clipboardStore.copySelection(region, grid)
       - cut(region): Call clipboardStore.copySelection(region, grid), then clearSelectedPixels(region)
       - clear(region): Call clearSelectedPixels(region)
       - invert(region): Call invertSelectedPixels(region)
    5. Helper functions:
       - clearSelectedPixels(region): Iterate grid keys in region, delete pixels (set to #FFFFFF or remove from Map)
       - invertSelectedPixels(region): Iterate grid keys in region, invert colors (use complementary color calculation or simple negation)

    Design: Separates gesture logic from UI components, reusable across SelectionOverlay and SelectionTools.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    useSelection exports gesture handler and operation functions, copy/cut/clear/invert work correctly, TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 4: Build SelectionOverlay component</name>
  <files>
    apps/mobile/src/components/selection/SelectionOverlay.tsx
  </files>
  <action>
    Create apps/mobile/src/components/selection/SelectionOverlay.tsx:

    1. Import: React, View, PanGestureHandler from 'react-native'
    2. Import: GestureHandlerRootView from 'react-native-gesture-handler'
    3. Import: useCanvasStore, { useSelectionGesture } from '../../hooks/useSelection'
    4. Component structure:
       - Absolute positioned overlay on top of CanvasGrid
       - Render selection rectangle if canvasStore.selectionRegion is not null
       - Selection rectangle: Blue border (2px), semi-transparent blue background
       - 4 corner handles (20x20px squares) for resizing selection
       - Use useSelectionGesture for drag-to-select gesture
    5. Coordinate calculation:
       - Convert selection region (grid coordinates) to screen coordinates
       - ScreenX = x1 * cellSize + panOffset.x
       - ScreenY = y1 * cellSize + panOffset.y
       - Width = (x2 - x1 + 1) * cellSize
       - Height = (y2 - y1 + 1) * cellSize
    6. Render selection tools toolbar below selection rectangle
    7. Hide selection overlay when currentTool is not 'select' (optional, or always show when selection exists)

    Design: Minimal visual overlay, clear blue border, drag handles at corners, toolbar anchored to selection.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    SelectionOverlay renders rectangle, drag-to-select works, handles resize selection, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 5: Build SelectionTools component</name>
  <files>
    apps/mobile/src/components/selection/SelectionTools.tsx
  </files>
  <action>
    Create apps/mobile/src/components/selection/SelectionTools.tsx:

    1. Import: React, View, TouchableOpacity, Text from 'react-native'
    2. Import: MaterialIcons from '@expo/vector-icons'
    3. Import: useCanvasStore, useClipboardStore, { useSelectionOperations } from '../../hooks/useSelection'
    4. Component structure:
       - Horizontal toolbar with 4 action buttons: Copy, Cut, Clear, Invert
       - Each button: Icon + label, 44x44px tap target
       - Positioned below or to the right of selection rectangle
    5. Button actions:
       - Copy: Call copy(selectionRegion)
       - Cut: Call cut(selectionRegion)
       - Clear: Call clear(selectionRegion)
       - Invert: Call invert(selectionRegion)
    6. Button styling:
       - Blue background with white text (matches tool drawer style)
       - Active state: Darker blue
       - Disabled state: Gray with opacity=0.5 (when no selection)
    7. Accessibility: accessibilityLabel for each button ("Copy selection", "Cut selection", etc.)
    8. Use React.memo for performance

    Design: Compact toolbar matching tool drawer button dimensions, positioned near selection.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    SelectionTools renders 4 buttons, all operations work (copy, cut, clear, invert), disabled when no selection, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 6: Add paste functionality to canvasStore</name>
  <files>
    apps/mobile/src/stores/canvasStore.ts
  </files>
  <action>
    Update apps/mobile/src/stores/canvasStore.ts:

    1. Add to CanvasState interface:
       - pasteClipboard: (x: number, y: number) => void
    2. Add pasteClipboard action:
       - Import useClipboardStore (inside action or via injection)
       - Get clipboard data from clipboardStore
       - Iterate through clipboard Map
       - Calculate paste position: newX = x + clipboardKeyX, newY = y + clipboardKeyY
       - Call setPixel(newX, newY, color) for each clipboard pixel
       - Constrain paste to grid bounds (clip pixels outside bounds)
    3. Trigger paste from SelectionTools:
       - Add "Paste" button to SelectionTools (or separate paste action)
       - On press: Call canvasStore.pasteClipboard(targetX, targetY)
       - Target position: Center of viewport or user tap position

    This enables pasting clipboard content anywhere on canvas.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    pasteClipboard action works, pastes clipboard pixels at target position, clips to grid bounds, TypeScript compiles
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **TypeScript compilation**: Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` — must pass with 0 errors
2. **Selection gesture**: Test that drag-to-select creates selection rectangle with correct bounds
3. **Copy operation**: Test that copy stores selection in clipboardStore
4. **Cut operation**: Test that cut copies selection and clears pixels from canvas
5. **Clear operation**: Test that clear removes pixels from selected region
6. **Invert operation**: Test that invert changes colors in selected region
7. **Paste operation**: Test that paste places clipboard content at target position
8. **Selection overlay**: Test that overlay renders correctly on top of canvas
</verification>

<success_criteria>
1. User can select rectangular region on canvas using drag-to-select gesture
2. User can copy selected region to clipboard via Copy button
3. User can cut selected region (copy + clear) via Cut button
4. User can clear selected region (remove all pixels) via Clear button
5. User can invert colors in selected region via Invert button
6. User can paste clipboard content to canvas at selected position via Paste button
7. Selection overlay renders on top of canvas with blue border and drag handles
8. Selection tools toolbar appears when selection exists and is disabled when no selection
9. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-颜色与高级特性/04-04-SUMMARY.md`
</output>
