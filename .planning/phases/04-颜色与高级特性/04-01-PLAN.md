---
phase: 04-颜色与高级特性
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/src/stores/canvasStore.ts
  - apps/mobile/src/data/palettes.ts
  - apps/mobile/src/stores/colorStore.ts
  - apps/mobile/src/components/color/PaletteGrid.tsx
  - apps/mobile/src/components/color/PaletteSelector.tsx
autonomous: true
requirements: [COLR-01, COLR-02, COLR-08]
user_setup: []
must_haves:
  truths:
    - User can select predefined Perler bead color palettes
    - User can switch between palette presets (48, 96, 144, 168 colors)
    - User can see colors organized by brand (MARD, COCO, 漫漫, 盼盼, 咪小窝)
    - User can select colors from palette for drawing
    - Palette data persists across app restarts
  artifacts:
    - path: "apps/mobile/src/data/palettes.ts"
      provides: "Perler bead color palettes with brand mappings"
      min_lines: 50
    - path: "apps/mobile/src/stores/colorStore.ts"
      provides: "Color state management with palette selection"
      exports: ["useColorStore", "ColorState"]
    - path: "apps/mobile/src/components/color/PaletteGrid.tsx"
      provides: "Color palette grid component"
      min_lines: 80
    - path: "apps/mobile/src/components/color/PaletteSelector.tsx"
      provides: "Palette preset selector (48/96/144/168)"
      min_lines: 60
  key_links:
    - from: "apps/mobile/src/data/palettes.ts"
      to: "colorSystemMapping.json"
      via: "Copy JSON data to TypeScript"
      pattern: "copy.*colorSystemMapping"
    - from: "apps/mobile/src/stores/colorStore.ts"
      to: "apps/mobile/src/data/palettes.ts"
      via: "Import palette data"
      pattern: "import.*from.*palettes"
    - from: "apps/mobile/src/components/color/PaletteGrid.tsx"
      to: "useColorStore"
      via: "Subscribe to selectedColor and currentPalette"
      pattern: "useColorStore\\(.*selectedColor.*\\)"
---

<objective>
Deliver predefined Perler bead color palette system with multiple presets and multi-brand support

Purpose: Users need access to realistic Perler bead colors organized by brand, with different palette sizes for different project needs. This is foundational for all color-related features.

Output: Working color palette system with brand-organized colors, selectable presets, and integration with drawing tools.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-高级编辑与用户体验/03-01-SUMMARY.md

# Existing codebase patterns
From apps/mobile/src/stores/canvasStore.ts:
```typescript
export const useCanvasStore = create<CanvasState>()(
  persist(
    (set, get) => ({
      selectedColor: '#000000',
      setSelectedColor: (color: ColorHex) => set({ selectedColor: color }),
      // ... other state
    }),
    { name: 'canvas-storage', storage: createJSONStorage(() => mmkvStorage) }
  )
);
```

From @pixelbead/color-system:
```typescript
export type { ColorHex, PaletteColor } from '@pixelbead/shared-types';
export function findClosestColor(targetHex: string, palette: PaletteColor[]): PaletteColor | null;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy color system mapping data to mobile app</name>
  <files>
    apps/mobile/src/data/palettes.ts
  </files>
  <action>
    Create apps/mobile/src/data/palettes.ts with:
    1. Copy colorSystemMapping.json content (29,063 bytes, multi-brand color codes)
    2. Export as const ColorSystemMapping: Record<string, Record<string, string>>
    3. Create helper functions:
       - createPaletteFromSystem(brand: ColorSystem, maxColors: 48 | 96 | 144 | 168): PaletteColor[]
       - groupColorsByLetter(colors: PaletteColor[]): Map<string, PaletteColor[]>
    4. Export const DEFAULT_COLORS from types.ts
    5. Add TypeScript types from @pixelbead/shared-types (ColorSystem, PaletteColor)

    Note: This is pure data copy + helper functions, no UI. Helper functions parse colorSystemMapping to extract brand-specific color lists with letter prefixes (A01, B02, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    File exists with ColorSystemMapping constant, helper functions compile without errors, export types match @pixelbead/shared-types
  </done>
</task>

<task type="auto">
  <name>Task 2: Create color store with palette management</name>
  <files>
    apps/mobile/src/stores/colorStore.ts
  </files>
  <action>
    Create apps/mobile/src/stores/colorStore.ts using Zustand pattern (matches canvasStore.ts):

    1. Import: create, persist, createJSONStorage from zustand/middleware, mmkvStorage from storage/mmkvStorage
    2. Define ColorState interface:
       - selectedPaletteSize: 48 | 96 | 144 | 168
       - selectedBrand: ColorSystem ('MARD' | 'COCO' | '漫漫' | '盼盼' | '咪小窝')
       - currentPalette: PaletteColor[]
       - paletteGroups: Array<{ letter: string; colors: PaletteColor[] }>
       - highlightedColor: ColorHex | null (for COLR-09)
    3. Create useColorStore with actions:
       - setSelectedPaletteSize(size): regenerates currentPalette using createPaletteFromSystem
       - setSelectedBrand(brand): regenerates currentPalette
       - selectColor(colorHex): updates selectedColor in canvasStore
       - setHighlightedColor(hex): for highlighting all pixels of a color
       - generatePalette(): internal function using palettes.ts helpers
    4. Persist with name: 'color-storage' using mmkvStorage
    5. Initialize: selectedPaletteSize=48, selectedBrand='MARD', call generatePalette on mount

    Pattern reference: Match canvasStore.ts structure for Zustand + MMKV persistence.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    Color store compiles, persists state to MMKV, generates palettes on mount, integrates with canvasStore.selectedColor
  </done>
</task>

<task type="auto">
  <name>Task 3: Build PaletteGrid component for color selection</name>
  <files>
    apps/mobile/src/components/color/PaletteGrid.tsx
  </files>
  <action>
    Create apps/mobile/src/components/color/PaletteGrid.tsx:

    1. Import: React, View, ScrollView, TouchableOpacity from 'react-native'
    2. Import: useColorStore, useCanvasStore
    3. Component props: none (subscribes to stores directly)
    4. Render structure:
       - Loop through colorStore.paletteGroups
       - For each group: letter header + grid of color buttons
       - Color button: TouchableOpacity with backgroundColor=hex, size=40px, margin=4px, borderRadius=4px
       - Selected color indicator: blue border (3px) around selectedColor
       - On press: call colorStore.selectColor(hex)
    5. Accessibility: Add accessibilityLabel with color hex and brand code
    6. Performance: Use React.memo to prevent unnecessary re-renders on palette selection changes
    7. Use FlatList for better performance with large palettes (144+ colors)

    Design: Letter group headers (A, B, C...), 5-6 colors per row, scrollable vertically.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    PaletteGrid renders colors grouped by letter, tap selects color, selected color shows blue border, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Build PaletteSelector for preset switching</name>
  <files>
    apps/mobile/src/components/color/PaletteSelector.tsx
  </files>
  <action>
    Create apps/mobile/src/components/color/PaletteSelector.tsx:

    1. Import: React, View, TouchableOpacity, Text from 'react-native'
    2. Import: useColorStore, MaterialIcons from '@expo/vector-icons'
    3. Component structure:
       - Horizontal scrollable row of preset buttons
       - Presets: [48, 96, 144, 168]
       - Brand selector dropdown (simple TouchableOpacity with icon)
       - Selected preset: blue background, white text
       - Unselected presets: gray background, black text
       - On preset press: colorStore.setSelectedPaletteSize(size)
    4. Brand selector: Show current brand name + down icon, on press show modal with 5 brand options (MARD, COCO, 漫漫, 盼盼, 咪小窝)
    5. Add accessibility labels: "48 color palette", "96 color palette", etc.
    6. Use React.memo for performance

    Design: Compact horizontal bar at top of color panel, same height as tool drawer buttons (~50px).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    PaletteSelector renders preset buttons, brand selector works, palette updates on selection, no TypeScript errors
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **TypeScript compilation**: Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` — must pass with 0 errors
2. **Color data integrity**: Verify colorSystemMapping has all brand codes (MARD, COCO, 漫漫, 盼盼, 咪小窝)
3. **Store persistence**: Test that palette selection persists after app restart (MMKV storage)
4. **Component rendering**: Verify PaletteGrid and PaletteSelector render without crashes
5. **Color selection integration**: Test that selecting color updates canvasStore.selectedColor
</verification>

<success_criteria>
1. User can select predefined Perler bead palettes (48, 96, 144, 168 colors) from PaletteSelector
2. User can switch between 5 brands (MARD, COCO, 漫漫, 盼盼, 咪小窝) and see brand-specific color codes
3. PaletteGrid displays colors organized by letter groups (A, B, C...) with visual selection indicator
4. Tapping a color in PaletteGrid updates the selected color for drawing tools
5. Color palette selection persists across app restarts via MMKV storage
6. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-颜色与高级特性/04-01-SUMMARY.md`
</output>
