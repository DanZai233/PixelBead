---
phase: 06-AI_素材广场与部署
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified: [apps/mobile/app.json, apps/mobile/eas.json, apps/mobile/assets/icon.png, apps/mobile/assets/adaptive-icon.png, apps/mobile/assets/splash.png, apps/mobile/assets/ios/*, apps/mobile/assets/android/*, apps/mobile/ios/PixelBead/Info.plist]
autonomous: true
requirements: [DEPL-01, DEPL-02, DEPL-03, DEPL-04, DEPL-05]
user_setup:
  - service: Apple Developer Program
    why: "iOS App Store distribution requires Apple Developer account"
    dashboard_config:
      - task: "Create App Store Connect app record"
        location: "App Store Connect -> My Apps -> +"
      - task: "Create bundle identifier"
        location: "Identifiers -> App IDs -> +"
      - task: "Enable capabilities"
        location: "App IDs -> PixelBead -> Capabilities"
  - service: Google Play Console
    why: "Android Play Store distribution requires Google Play Developer account"
    dashboard_config:
      - task: "Create Play Console app"
        location: "Google Play Console -> All apps -> Create app"
      - task: "Configure app signing"
        location: "Setup -> App integrity -> App signing key"
      - task: "Generate signing key"
        location: "Setup -> App integrity -> Upload key"
  - service: EAS (Expo Application Services)
    why: "Automated iOS and Android builds via cloud"
    dashboard_config:
      - task: "Link GitHub repository"
        location: "EAS Dashboard -> Projects -> Link repository"
      - task: "Configure build profiles"
        location: "EAS Dashboard -> Projects -> PixelBead -> Build profiles"
      - task: "Set up environment variables"
        location: "EAS Dashboard -> Projects -> PixelBead -> Secrets"

must_haves:
  truths:
    - App has proper app icon for iOS and Android
    - App has splash screen for iOS and Android
    - App version and build numbers configured correctly
    - App builds successfully for iOS App Store via EAS
    - App builds successfully for Google Play Store via EAS
    - App metadata follows App Store and Google Play guidelines
  artifacts:
    - path: "apps/mobile/app.json"
      provides: "App configuration with icons, splash screens, versioning"
      contains: ["icon", "splash", "version", "ios", "android"]
    - path: "apps/mobile/eas.json"
      provides: "EAS build configuration for iOS and Android"
      contains: ["builds", "cli", "submit"]
    - path: "apps/mobile/assets/icon.png"
      provides: "App icon (1024x1024 PNG)"
    - path: "apps/mobile/assets/splash.png"
      provides: "Splash screen (1284x2778 PNG)"
    - path: "apps/mobile/ios/PixelBead/Info.plist"
      provides: "iOS app metadata (permissions, display name, bundle ID)"
      contains: ["CFBundleDisplayName", "NSPhotoLibraryUsageDescription", "NSCameraUsageDescription"]
    - path: "apps/mobile/android/app/src/main/AndroidManifest.xml"
      provides: "Android app metadata (permissions, package name)"
      contains: ["package", "uses-permission"]
  key_links:
    - from: "apps/mobile/eas.json"
      to: "https://eas-api.expo.dev"
      via: "EAS Build API"
      pattern: "eas build --platform ios"
    - from: "apps/mobile/app.json"
      to: "apps/mobile/eas.json"
      via: "App configuration referenced by EAS"
      pattern: "\"app\".*app\\.json"
    - from: "apps/mobile/assets/icon.png"
      to: "apps/mobile/app.json"
      via: "Icon configuration"
      pattern: "\"icon\".*assets/icon.png"
---

<objective>
Prepare app for iOS App Store and Google Play Store distribution with proper icons, splash screens, versioning, and EAS build configuration.

Purpose: Enable automated building and submission to app stores following submission guidelines.
Output: Configured app with icons, splash screens, versioning, and working EAS builds.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key configurations and guidelines. -->

App Store submission requirements:
- App icon: 1024x1024 PNG (no transparency, no rounded corners)
- Screenshots: iPhone 6.7" display (1290x2796), iPhone 6.5" display (1242x2688)
- App preview videos: Optional, 15-30 seconds
- Privacy policy URL: Required if app collects data
- Bundle identifier: com.danzai.pixelbead (reverse domain format)
- Version: X.Y.Z format (e.g., 1.0.0)
- Build number: Incrementing integer (auto-incremented by EAS)
- Permissions: Photo library, Camera (explain why in usage descriptions)

Google Play submission requirements:
- App icon: 512x512 PNG (no transparency)
- Feature graphic: 1024x500 PNG (store listing)
- Screenshots: Phone (16:9 or 9:16), Tablet
- Privacy policy URL: Required if app collects data
- Package name: com.danzai.pixelbead (reverse domain format)
- Version: X.Y.Z format (e.g., 1.0.0)
- Version code: Incrementing integer (auto-incremented by EAS)
- Permissions: Camera, Read/Write external storage (auto-added by expo-image-picker)

EAS build profiles:
- production: Final app store builds
- preview: TestFlight / Internal testing builds
- development: Development builds with DevClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app icon</name>
  <files>apps/mobile/assets/icon.png</files>
  <action>Create or verify app icon (1024x1024 PNG):

1. Create icon.png in assets/ directory:
   - Size: 1024x1024 pixels
   - Format: PNG
   - Background: Solid color or gradient
   - Design: Pixel art bead or grid icon (appropriate for pixel art tool)
   - No transparency
   - No rounded corners (OS handles rounding)
   - Minimalist, recognizable at small sizes

2. If icon already exists:
   - Verify dimensions: 1024x1024
   - Verify format: PNG
   - Verify quality: Clear, no artifacts

3. Design guidelines:
   - Use app's color palette (from PixelBead branding)
   - Simple, bold icon that scales well
   - Pixel art style or grid pattern (matches app purpose)
   - Consider using one of the predefined Perler bead colors

4. Export icon.png to assets/ directory

Note: EAS will auto-generate all required icon sizes from this 1024x1024 source.
</action>
  <verify>
    <automated>ls -lh apps/mobile/assets/icon.png && file apps/mobile/assets/icon.png</automated>
  </verify>
  <done>App icon exists at 1024x1024 PNG format</done>
</task>

<task type="auto">
  <name>Task 2: Create adaptive icon (Android)</name>
  <files>apps/mobile/assets/adaptive-icon.png</files>
  <action>Create or verify adaptive icon for Android (foreground):

1. Create adaptive-icon.png in assets/ directory:
   - Size: 1024x1024 pixels
   - Format: PNG
   - Design: Icon on transparent background
   - Safe zone: Center 66% of icon (masked on many devices)
   - No outer padding needed (EAS generates mask)

2. If adaptive icon already exists:
   - Verify dimensions: 1024x1024
   - Verify format: PNG
   - Verify transparency: Outer area transparent

3. Design guidelines:
   - Use same design as main icon
   - Place important elements in center 66% of canvas
   - Outer 17% on each side will be masked on some Android devices
   - Background color configured in app.json (iconBackgroundColor)

4. Export adaptive-icon.png to assets/ directory

5. Create iconBackgroundColor configuration:
   - Add to app.json: "iconBackgroundColor": "#1a1a1a" (or brand color)
   - This is shown when icon is masked on Android

Note: EAS will auto-generate adaptive icon sizes and background.
</action>
  <verify>
    <automated>ls -lh apps/mobile/assets/adaptive-icon.png && file apps/mobile/assets/adaptive-icon.png</automated>
  </verify>
  <done>Adaptive icon exists at 1024x1024 PNG with transparent background</done>
</task>

<task type="auto">
  <name>Task 3: Create splash screen</name>
  <files>apps/mobile/assets/splash.png</files>
  <action>Create or verify splash screen (1284x2778 PNG):

1. Create splash.png in assets/ directory:
   - Size: 1284x2778 pixels (iPhone 14 Pro Max)
   - Format: PNG
   - Background: Brand color or gradient
   - Center: App logo or icon
   - No transparency needed (full screen)

2. If splash already exists:
   - Verify dimensions: 1284x2778
   - Verify format: PNG
   - Verify quality: Clear, no artifacts

3. Design guidelines:
   - Use app's brand colors
   - Center app logo or icon
   - Add app name below logo (optional)
   - Keep design simple (fast loading perception)
   - Avoid too much text (splash disappears quickly)

4. Export splash.png to assets/ directory

Note: EAS will auto-generate splash screen sizes for all devices.
</action>
  <verify>
    <automated>ls -lh apps/mobile/assets/splash.png && file apps/mobile/assets/splash.png</automated>
  </verify>
  <done>Splash screen exists at 1284x2778 PNG format</done>
</task>

<task type="auto">
  <name>Task 4: Configure app.json with icons, splash, version</name>
  <files>apps/mobile/app.json</files>
  <action>Configure app.json with icons, splash screens, and versioning:

1. Add icon configuration:
   ```json
   "icon": "./assets/icon.png",
   "adaptiveIcon": {
     "foregroundImage": "./assets/adaptive-icon.png",
     "backgroundColor": "#1a1a1a"
   }
   ```

2. Add splash configuration:
   ```json
   "splash": {
     "image": "./assets/splash.png",
     "resizeMode": "contain",
     "backgroundColor": "#1a1a1a"
   }
   ```

3. Configure deep linking (if not already configured in plan 04):
   ```json
   "scheme": "pixelbead",
   "ios": {
     "associatedDomains": ["applinks:pindou.danzaii.cn"]
   },
   "android": {
     "intentFilters": [
       {
         "action": "VIEW",
         "data": {
           "scheme": "https",
           "host": "pindou.danzaii.cn",
           "pathPrefix": "/share"
         }
       }
     ]
   }
   ```

4. Configure versioning:
   ```json
   "version": "1.0.0",
   "ios": {
     "buildNumber": "1",
     "bundleIdentifier": "com.danzai.pixelbead",
     "infoPlist": {
       "CFBundleDisplayName": "PixelBead",
       "NSPhotoLibraryUsageDescription": "PixelBead needs access to your photo library to import images for pixel art conversion.",
       "NSCameraUsageDescription": "PixelBead needs access to your camera to capture images for pixel art conversion."
     }
   },
   "android": {
     "package": "com.danzai.pixelbead",
     "versionCode": 1,
     "permissions": ["CAMERA", "READ_EXTERNAL_STORAGE", "WRITE_EXTERNAL_STORAGE"]
   }
   ```

5. Configure app metadata:
   ```json
   "name": "PixelBead",
   "slug": "pixelbead",
   "description": "Design Perler bead pixel art on the go with PixelBead. Create, save, and share your pixel art designs anytime, anywhere.",
   "privacy": "public"
   ```

6. Update existing configuration:
   - Preserve existing scheme, jsEngine, newArchEnabled from plan 05-03
   - Add new configurations (icon, splash, version, ios, android)
   - Ensure deep linking from plan 04 is preserved

7. Verify all fields are present and correct.

Update existing app.json from previous plans.
</action>
  <verify>
    <automated>grep -n "icon.*assets/icon\|splash.*assets/splash\|version.*1.0.0\|bundleIdentifier.*pixelbead" apps/mobile/app.json</automated>
  </verify>
  <done>App.json configured with icons, splash screens, versioning, and metadata</done>
</task>

<task type="auto">
  <name>Task 5: Create EAS build configuration</name>
  <files>apps/mobile/eas.json</files>
  <action>Create or update EAS build configuration for automated builds:

1. Create eas.json in root of mobile/ directory:
   ```json
   {
     "cli": {
       "version": ">= 7.0.0"
     },
     "build": {
       "development": {
         "developmentClient": true,
         "distribution": "internal",
         "ios": {
           "simulator": true
         }
       },
       "preview": {
         "distribution": "internal",
         "ios": {
           "buildConfiguration": "Release"
         },
         "android": {
           "buildType": "apk"
         }
       },
       "production": {
         "ios": {
           "buildConfiguration": "Release",
           "autoIncrement": true
         },
         "android": {
           "buildType": "app-bundle",
           "autoIncrement": true
         }
       }
     },
     "submit": {
       "production": {
         "ios": {
           "appleId": "...", // User must fill in
           "ascAppId": "...", // User must fill in
           "appleTeamId": "..." // User must fill in
         },
         "android": {
           "serviceAccountKeyPath": "./service-account.json", // User must provide
           "track": "internal" // Change to "production" for public release
         }
       }
     }
   }
   ```

2. Build profile explanations:
   - development: DevClient builds for development (fast iterations)
   - preview: TestFlight / Internal testing builds (beta testing)
   - production: App Store / Play Store builds (final release)

3. Build commands:
   - iOS development: eas build --profile development --platform ios
   - Android development: eas build --profile development --platform android
   - iOS preview: eas build --profile preview --platform ios
   - Android preview: eas build --profile preview --platform android
   - iOS production: eas build --profile production --platform ios
   - Android production: eas build --profile production --platform android

4. Submit commands (after build):
   - iOS submit: eas submit --platform ios --profile production
   - Android submit: eas submit --platform android --profile production

5. Note: User must configure Apple ID, App Store Connect app ID, and Android service account key before submitting.

If eas.json already exists, update it with production and submit configurations.
</action>
  <verify>
    <automated>grep -n "production\|preview\|development" apps/mobile/eas.json</automated>
  </verify>
  <done>EAS build configuration created with development, preview, and production profiles</done>
</task>

<task type="auto">
  <name>Task 6: Update iOS Info.plist with permissions</name>
  <files>apps/mobile/ios/PixelBead/Info.plist</files>
  <action>Verify and update Info.plist with required permissions and metadata:

1. Check existing Info.plist:
   - Verify CFBundleDisplayName: PixelBead
   - Verify CFBundleIdentifier: com.danzai.pixelbead
   - Verify CFBundleVersion and CFBundleShortVersionString

2. Add camera permission:
   ```xml
   <key>NSCameraUsageDescription</key>
   <string>PixelBead needs access to your camera to capture images for pixel art conversion.</string>
   ```

3. Add photo library permission:
   ```xml
   <key>NSPhotoLibraryUsageDescription</key>
   <string>PixelBead needs access to your photo library to import images for pixel art conversion.</string>
   ```

4. Add Face ID (if needed for secure storage, optional):
   ```xml
   <key>NSFaceIDUsageDescription</key>
   <string>PixelBead uses Face ID to secure your AI API keys.</string>
   ```

5. Verify other metadata:
   - CFBundleName: PixelBead
   - CFBundleDisplayName: 拼豆糕手 (Chinese name for App Store)
   - CFBundleVersion: 1 (build number)
   - CFBundleShortVersionString: 1.0.0 (version)

6. Ensure all permissions have user-friendly descriptions explaining why app needs each permission.

Note: Many of these may already be configured by Expo. Verify they are present and correct.
</action>
  <verify>
    <automated>grep -n "NSCameraUsageDescription\|NSPhotoLibraryUsageDescription\|CFBundleDisplayName" apps/mobile/ios/PixelBead/Info.plist</automated>
  </verify>
  <done>Info.plist configured with permissions, display name, and bundle identifier</done>
</task>

<task type="auto">
  <name>Task 7: Update Android Manifest with permissions</name>
  <files>apps/mobile/android/app/src/main/AndroidManifest.xml</files>
  <action>Verify and update AndroidManifest.xml with permissions and metadata:

1. Check existing AndroidManifest.xml:
   - Verify package name: com.danzai.pixelbead
   - Verify versionCode and versionName

2. Add camera permission:
   ```xml
   <uses-permission android:name="android.permission.CAMERA" />
   ```

3. Add storage permissions (if not already auto-added by expo-image-picker):
   ```xml
   <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
   <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
   ```

4. Add internet permission (should already exist):
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   ```

5. Verify app metadata:
   - android:label="@string/app_name" (should be "PixelBead")
   - android:roundIcon="@mipmap/ic_launcher_round"
   - android:icon="@mipmap/ic_launcher"

6. Update strings.xml (apps/mobile/android/app/src/main/res/values/strings.xml):
   ```xml
   <string name="app_name">PixelBead</string>
   ```

7. Ensure Chinese name for Play Store (in metadata, not manifest):
   - Play Store allows separate localized names
   - Configure in Google Play Console after upload

Note: Many permissions and metadata may already be configured by Expo. Verify and update as needed.
</action>
  <verify>
    <automated>grep -n "uses-permission.*CAMERA\|package.*pixelbead\|app_name" apps/mobile/android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>AndroidManifest.xml configured with permissions, package name, and metadata</done>
</task>

<task type="auto">
  <name>Task 8: Create app store metadata document</name>
  <files>apps/mobile/APP_STORE_METADATA.md</files>
  <action>Create document with app store submission metadata:

1. Create APP_STORE_METADATA.md with:

## iOS App Store Metadata

**App Name:** PixelBead (English), 拼豆糕手 (Chinese)
**Bundle Identifier:** com.danzai.pixelbead
**Version:** 1.0.0
**Category:** Graphics & Design / Photo & Video

**Short Description (80 characters):**
Design Perler bead pixel art on the go.

**Long Description (4000 characters):**
Create beautiful Perler bead pixel art designs anytime, anywhere with PixelBead. Whether you're a beginner or an expert pixel artist, PixelBead provides all the tools you need to bring your creative ideas to life.

**Key Features:**
• High-performance canvas editor (up to 200x200 grids at 60 FPS)
• Professional drawing tools: brush, eraser, fill, line, rectangle, circle
• Intelligent color management: predefined Perler bead palettes, custom colors, HSL picker
• Image import: convert photos to pixel art automatically with cropping and background removal
• AI-powered generation: create pixel art from text prompts and reference images
• Material gallery: browse and load community-created designs
• Offline support: core editing features work without internet
• Share and export: PNG export, share links, JSON project files
• Fully accessible: screen reader support, dynamic text sizing

**Keywords:** Pixel art, Perler beads, Hama beads, pixel editor, pixel drawing, bead art, iron beads, fuse beads, creative tool, offline editor

**Support URL:** https://pindou.danzaii.cn/support
**Marketing URL:** https://pindou.danzaii.cn
**Privacy Policy URL:** https://pindou.danzaii.cn/privacy

**Screenshots Required:**
- iPhone 6.7" display (1290x2796)
- iPhone 6.5" display (1242x2688)
- Show editor with tools, color palette, project gallery, export options

## Google Play Store Metadata

**App Name:** PixelBead
**Package Name:** com.danzai.pixelbead
**Version:** 1.0.0 (Version Code: 1)
**Category:** Art & Design

**Short Description (80 characters):**
Design Perler bead pixel art on the go.

**Full Description (4000 characters):**
[Same as iOS long description]

**Keywords:** Pixel art, Perler beads, Hama beads, pixel editor, pixel drawing, bead art, creative tool, offline editor

**Privacy Policy URL:** https://pindou.danzaii.cn/privacy

**Store Listing Assets Required:**
- High-res icon: 512x512 PNG
- Feature graphic: 1024x500 PNG (store listing hero)
- Screenshots: Phone (16:9 or 9:16), optional Tablet

**Content Rating:** Everyone (pixel art creation tool, no objectionable content)

2. Save to apps/mobile/APP_STORE_METADATA.md

This document guides user through app store submission process.
</action>
  <verify>
    <automated>grep -n "PixelBead\|Bundle Identifier\|Package Name\|Privacy Policy" apps/mobile/APP_STORE_METADATA.md</automated>
  </verify>
  <done>App store metadata document created with all required information</done>
</task>

</tasks>

<verification>
- App icon exists at 1024x1024 PNG with proper design
- Adaptive icon exists at 1024x1024 PNG with transparent background
- Splash screen exists at 1284x2778 PNG with proper design
- App.json configured with icons, splash screens, version 1.0.0, and metadata
- EAS build configuration created with development, preview, and production profiles
- Info.plist configured with permissions and display name
- AndroidManifest.xml configured with permissions and package name
- App store metadata document created with submission guidelines
- TypeScript compilation passes with 0 errors
</verification>

<success_criteria>
1. App icon, adaptive icon, and splash screen exist in correct sizes and formats
2. App.json configured with all icons, splash screens, versioning, and metadata
3. EAS build configuration includes development, preview, and production profiles
4. Info.plist and AndroidManifest.xml have required permissions and metadata
5. App store metadata document provides complete submission guidelines
6. TypeScript compilation passes with 0 errors
7. User can run `eas build --profile production --platform ios` and `eas build --profile production --platform android` to build for app stores
</success_criteria>

<output>
After completion, create `.planning/phases/06-AI_素材广场与部署/06-05-SUMMARY.md`
</output>
