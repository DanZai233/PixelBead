---
phase: 06-AI_素材广场与部署
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/mobile/src/services/aiService.ts, apps/mobile/src/stores/aiStore.ts, apps/mobile/src/components/ai/AiGenerateModal.tsx, apps/mobile/src/components/ai/AiSettingsModal.tsx, apps/mobile/app/(tabs)/editor.tsx, apps/mobile/package.json]
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, AI-05]
user_setup:
  - service: OpenAI
    why: "Default AI provider for image generation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "https://platform.openai.com/api-keys"
  - service: DeepSeek
    why: "Alternative AI provider for text-to-image generation"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "https://platform.deepseek.com/"
  - service: Gemini
    why: "Google AI provider with multimodal capabilities"
    env_vars:
      - name: GEMINI_API_KEY
        source: "https://makersuite.google.com/app/apikey"

must_haves:
  truths:
    - User can input text prompt to generate pixel art
    - User can configure AI provider settings (provider, API key, model, endpoint)
    - User can upload reference image from device library for AI generation
    - User can specify canvas size for generated pixel art
    - User can select AI model from configured provider
    - Generated image loads into canvas and resizes grid accordingly
  artifacts:
    - path: "apps/mobile/src/services/aiService.ts"
      provides: "AI generation with multiple provider support"
      exports: ["generatePixelArtImage", "validateApiKey"]
    - path: "apps/mobile/src/stores/aiStore.ts"
      provides: "AI configuration state with MMKV persistence"
      contains: ["provider", "apiKey", "model", "imageUrlModel", "selectedModel"]
    - path: "apps/mobile/src/components/ai/AiGenerateModal.tsx"
      provides: "UI for AI generation with text prompt and reference image"
    - path: "apps/mobile/src/components/ai/AiSettingsModal.tsx"
      provides: "UI for AI provider configuration"
  key_links:
    - from: "apps/mobile/src/components/ai/AiGenerateModal.tsx"
      to: "apps/mobile/src/services/aiService.ts"
      via: "generatePixelArtImage() call"
      pattern: "generatePixelArtImage\\(prompt, config, referenceImage\\)"
    - from: "apps/mobile/src/stores/aiStore.ts"
      to: "apps/mobile/src/stores/aiStore.ts"
      via: "MMKV persistence middleware"
      pattern: "persist\\(\\{.*name.*ai.*storage.*mmkv\\)"
    - from: "apps/mobile/src/stores/aiStore.ts"
      to: "apps/mobile/src/stores/canvasStore.ts"
      via: "Generated image loaded into canvas"
      pattern: "loadGeneratedImage\\(imageData, width, height\\)"
---

<objective>
Implement AI generation system with support for OpenAI, DeepSeek, and Gemini providers, enabling users to generate pixel art from text prompts and reference images with configurable canvas sizes.

Purpose: Provide competitive AI-powered design creation feature matching web app capabilities.
Output: Working AI generation service with UI for configuration and image generation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/mobile/src/services/aiService.ts (web app reference)
@packages/shared-types/src/index.ts (AI types)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/shared-types/src/index.ts:
```typescript
export enum AIProvider {
  OPENAI = 'OPENAI',
  OPENROUTER = 'OPENROUTER',
  DEEPSEEK = 'DEEPSEEK',
  VOLCENGINE = 'VOLCENGINE',
  GEMINI = 'GEMINI',
  CUSTOM = 'CUSTOM',
}

export interface AIConfig {
  provider: AIProvider;
  apiKey: string;
  model?: string;
  baseUrl?: string;
  endpoint?: string;
  imageUrlModel?: string;
}

export const AI_MODELS = {
  [AIProvider.OPENAI]: [
    { id: 'gpt-4o', name: 'GPT-4o' },
    { id: 'gpt-4o-mini', name: 'GPT-4o Mini' },
    { id: 'dall-e-3', name: 'DALL-E 3', isImageModel: true },
  ],
  [AIProvider.DEEPSEEK]: [
    { id: 'deepseek-chat', name: 'DeepSeek Chat' },
  ],
  [AIProvider.GEMINI]: [
    { id: 'gemini-2.0-flash-exp', name: 'Gemini 2.0 Flash', isImageModel: true },
  ],
  // ... other providers
};

export const DEFAULT_ENDPOINTS = {
  [AIProvider.OPENAI]: 'https://api.openai.com/v1',
  [AIProvider.DEEPSEEK]: 'https://api.deepseek.com/v1',
  [AIProvider.GEMINI]: '',
  // ... other providers
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI service for mobile</name>
  <files>apps/mobile/src/services/aiService.ts</files>
  <action>Create AI generation service matching web app patterns with mobile adaptations:

1. Import types from @pixelbead/shared-types:
   - AIProvider, AIConfig, AI_MODELS, DEFAULT_ENDPOINTS

2. Implement generatePixelArtImage(prompt, config, referenceImage):
   - Parse model from config.model or AI_MODELS[config.provider][0]
   - Parse endpoint from config.endpoint or DEFAULT_ENDPOINTS[config.provider]
   - Switch on config.provider (OPENAI, DEEPSEEK, GEMINI)
   - For DEEPSEEK: throw error "DeepSeek 目前不支持图像生成" (same as web)
   - For OPENAI/OPENROUTER: POST to /chat/completions, process tool_calls for dalle.text2im, POST to /images/generations
   - For VOLCENGINE: POST to /images/generations with reference_images support
   - For GEMINI: POST to models/{model}:generateContent with image parts
   - Handle reference image: convert base64 data URL to proper format
   - Return base64 data URL: "data:image/png;base64,{data}"

3. Implement validateApiKey(provider, apiKey, endpoint):
   - Call generatePixelArtImage('test', { provider, apiKey, endpoint })
   - Return true on success, false on error (catch)

4. Error handling:
   - Parse error messages from each provider
   - Throw descriptive errors for API failures
   - Use try/catch with console.error

Use web app aiService.ts as reference pattern, adapt to React Native fetch API (same API).
</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AI service compiles and exports generatePixelArtImage and validateApiKey</done>
</task>

<task type="auto">
  <name>Task 2: Create AI configuration store</name>
  <files>apps/mobile/src/stores/aiStore.ts</files>
  <action>Create Zustand store with MMKV persistence for AI configuration:

1. Import:
   - create from zustand
   - MMKV from react-native-mmkv
   - persist middleware from zustand/middleware
   - AIProvider, AIConfig, AI_MODELS, DEFAULT_ENDPOINTS from @pixelbead/shared-types

2. Create MMKV storage instance:
   const storage = new MMKV()

3. Define aiStore with:
   - provider: AIProvider = AIProvider.OPENAI
   - apiKey: string = ''
   - endpoint: string = ''
   - model: string = ''
   - imageUrlModel: string = ''
   - favorites: string[] = [] (for AI-generated prompts, optional)

4. Actions:
   - setProvider(provider: AIProvider)
   - setApiKey(key: string)
   - setEndpoint(endpoint: string)
   - setModel(model: string)
   - setImageUrlModel(model: string)
   - resetConfig() (clear all fields)
   - getModelsForProvider() returns AI_MODELS[provider] filtered for image models
   - getAvailableModels() returns AI_MODELS (full list)

5. Persist with MMKV:
   persist({ name: 'ai-storage', storage: { ...MMKVStorageAdapter ... } })
   Use standard Zustand MMKV adapter pattern (see other stores)

Export useAiStore.
</action>
  <verify>
    <automated>grep -n "export.*useAiStore" apps/mobile/src/stores/aiStore.ts</automated>
  </verify>
  <done>AI store persists configuration across app restarts and exports all required actions</done>
</task>

<task type="auto">
  <name>Task 3: Create AI generation modal UI</name>
  <files>apps/mobile/src/components/ai/AiGenerateModal.tsx</files>
  <action>Create modal for AI generation with text prompt, reference image, and canvas size:

1. Import:
   - useState, useEffect from react
   - View, Text, TextInput, TouchableOpacity, ScrollView, Image from react-native
   - Modal, Alert from react-native
   - MaterialIcons from @expo/vector-icons
   - * as ImagePicker from expo-image-picker
   - { useAiStore } from stores/aiStore
   - { useCanvasStore } from stores/canvasStore (for loading generated image)
   - { generatePixelArtImage } from services/aiService

2. Component state:
   - prompt (string)
   - referenceImage (string | null)
   - isGenerating (boolean)
   - error (string | null)
   - canvasSize: { width: number; height: number }
   - showImagePicker (boolean)

3. UI layout (ScrollView):
   - Header: "Generate Pixel Art" with close button
   - Prompt input: multiline TextInput, placeholder "Describe your pixel art..."
   - Reference image section: button to pick image, preview if selected
   - Canvas size: width/height inputs (default 16x16, range 4-200), preset buttons (8x8, 16x16, 32x32)
   - Generate button: disabled if generating or no prompt, "Generate" / "Generating..."
   - Error message: Alert if error occurs
   - Loading spinner when isGenerating

4. Image picker integration:
   - Use ImagePicker.launchImageLibraryAsync({ mediaTypes: 'images', allowsEditing: true, base64: true })
   - On success: set referenceImage = `data:image/jpeg;base64,${assets[0].base64}`
   - Cancel button to remove reference image

5. Generate logic:
   - Get config from useAiStore (provider, apiKey, endpoint, model)
   - Call generatePixelArtImage(prompt, config, referenceImage)
   - On success: useCanvasStore.loadGeneratedImage(imageData, canvasSize.width, canvasSize.height)
   - Close modal on success
   - On error: Alert.alert("Generation Failed", error.message)
   - Set isGenerating false

6. Accessibility:
   - accessibilityLabel for all inputs and buttons
   - accessibilityRole="button" for interactive elements
   - accessibilityHint for actions

Modal slides up from bottom (standard pattern).
</action>
  <verify>
    <automated>grep -n "useCanvasStore.*loadGeneratedImage\|generatePixelArtImage" apps/mobile/src/components/ai/AiGenerateModal.tsx</automated>
  </verify>
  <done>AI modal prompts for text, uploads reference image, generates pixel art, loads into canvas</done>
</task>

<task type="auto">
  <name>Task 4: Create AI settings modal UI</name>
  <files>apps/mobile/src/components/ai/AiSettingsModal.tsx</files>
  <action>Create modal for AI provider configuration:

1. Import:
   - useState from react
   - View, Text, TextInput, TouchableOpacity, ScrollView from react-native
   - Modal from react-native
   - MaterialIcons from @expo/vector-icons
   - { useAiStore } from stores/aiStore
   - { validateApiKey } from services/aiService
   - AIProvider, AI_MODELS, DEFAULT_ENDPOINTS from @pixelbead/shared-types

2. Component state:
   - isValidating (boolean)
   - validationError (string | null)

3. UI layout (ScrollView):
   - Header: "AI Settings" with close button
   - Provider selection: buttons for OPENAI, DEEPSEEK, GEMINI (highlight selected)
   - API Key input: secureTextEntry, placeholder "Enter API key"
   - Endpoint input: placeholder for custom endpoint (optional)
   - Model selection: dropdown or buttons from getModelsForProvider()
   - Save button: saves to aiStore

4. Provider selection logic:
   - On tap: setProvider(provider), set endpoint to DEFAULT_ENDPOINTS[provider], set model to first image model
   - For DEEPSEEK: show warning "DeepSeek 目前不支持图像生成"

5. Model selection:
   - Get available models from aiStore.getModelsForProvider()
   - Filter for isImageModel: true (only image generation models)
   - Radio buttons for model selection

6. Validate API key:
   - Add "Test API Key" button next to API key input
   - On tap: set isValidating true, call validateApiKey(provider, apiKey, endpoint)
   - On success: Alert.alert("Valid", "API key is valid"), set isValidating false
   - On error: Alert.alert("Invalid", "API key is invalid"), set validationError, set isValidating false

7. Save logic:
   - Call aiStore.setProvider, setApiKey, setEndpoint, setModel
   - Close modal

8. Accessibility:
   - accessibilityLabel for all inputs and buttons
   - accessibilityRole="button" for provider selection
   - accessibilityHint for validation

Modal slides up from bottom.
</action>
  <verify>
    <automated>grep -n "setProvider\|setApiKey\|setModel\|validateApiKey" apps/mobile/src/components/ai/AiSettingsModal.tsx</automated>
  </verify>
  <done>AI settings modal configures provider, API key, endpoint, model with validation</done>
</task>

<task type="auto">
  <name>Task 5: Integrate AI modals into EditorScreen</name>
  <files>apps/mobile/app/(tabs)/editor.tsx</files>
  <action>Integrate AI generation and settings modals into editor screen:

1. Import:
   - AiGenerateModal from components/ai/AiGenerateModal
   - AiSettingsModal from components/ai/AiSettingsModal
   - useState from react

2. Add state:
   - showAiGenerate (boolean)
   - showAiSettings (boolean)

3. Add to editor screen tool bar (below export button):
   - Generate button (44x44px): MaterialIcons.auto_awesome icon
   - AI Settings button (44x44px): MaterialIcons.settings_suggest icon
   - Accessibility labels: "Generate pixel art with AI", "Configure AI settings"

4. Render modals:
   - {showAiGenerate && <AiGenerateModal visible={showAiGenerate} onClose={() => setShowAiGenerate(false)} />}
   - {showAiSettings && <AiSettingsModal visible={showAiSettings} onClose={() => setShowAiSettings(false)} />}

5. Add loadGeneratedImage to canvasStore (if not exists):
   - loadGeneratedImage(imageData: string, width: number, height: number)
   - Decode image data (base64 to buffer)
   - Import imageProcessor.ts (existing utility)
   - Use imageProcessor.resizeAndPixelateImage to convert to grid
   - Set canvas grid to result
   - Set canvas dimensions to width/height
   - Reset undo history

6. Position buttons in tool drawer or as floating action buttons below tool drawer
   - Consistent with existing button layout
   - Touch feedback with haptics (expo-haptics)

7. Accessibility:
   - accessibilityLabel for generate button: "Generate pixel art with AI"
   - accessibilityLabel for settings button: "Configure AI settings"
   - accessibilityHint: "Double tap to open AI generation"
</action>
  <verify>
    <automated>grep -n "AiGenerateModal\|AiSettingsModal\|loadGeneratedImage" apps/mobile/app/(tabs)/editor.tsx</automated>
  </verify>
  <done>AI generation and settings buttons accessible from editor, modals render correctly</done>
</task>

</tasks>

<verification>
- AI service generates pixel art from text prompts for OpenAI, Gemini providers
- Reference image upload works via expo-image-picker
- Canvas size specification works and grid resizes correctly
- Generated image loads into canvas and displays correctly
- AI settings modal configures provider, API key, endpoint, model
- API key validation works with success/error feedback
- AI configuration persists across app restarts (MMKV)
- TypeScript compilation passes with 0 errors
</verification>

<success_criteria>
1. User can tap Generate button, enter text prompt, optionally upload reference image, specify canvas size, and generate pixel art
2. User can tap AI Settings button, select provider (OpenAI, Gemini), enter API key, select model, and save configuration
3. Generated image loads into canvas and grid resizes to specified dimensions
4. AI configuration persists across app restarts
5. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-AI_素材广场与部署/06-01-SUMMARY.md`
</output>
