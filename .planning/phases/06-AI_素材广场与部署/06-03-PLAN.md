---
phase: 06-AI_素材广场与部署
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified: [apps/mobile/src/components/gallery/GalleryDetailScreen.tsx, apps/mobile/src/components/gallery/PublishModal.tsx, apps/mobile/app/(tabs)/editor.tsx, apps/mobile/src/stores/galleryStore.ts, apps/mobile/src/services/galleryService.ts, apps/mobile/src/stores/canvasStore.ts]
autonomous: true
requirements: [GALL-02, GALL-03, GALL-04, GALL-05]
user_setup: []

must_haves:
  truths:
    - User can like designs in gallery (local favorite marking)
    - User can favorite designs for quick access (MMKV persistence)
    - User can publish own design to gallery (title, author, tags)
    - User can load design from gallery into canvas
    - Gallery detail screen shows full design with preview
  artifacts:
    - path: "apps/mobile/src/components/gallery/GalleryDetailScreen.tsx"
      provides: "Detail view with like, favorite, load buttons"
    - path: "apps/mobile/src/components/gallery/PublishModal.tsx"
      provides: "Modal for publishing design with metadata"
    - path: "apps/mobile/src/stores/galleryStore.ts"
      provides: "Like and favorite actions with MMKV persistence"
      contains: ["toggleFavorite", "likeMaterial", "publishMaterial"]
    - path: "apps/mobile/src/stores/canvasStore.ts"
      provides: "loadMaterialIntoCanvas action"
      contains: ["loadMaterialIntoCanvas"]
  key_links:
    - from: "apps/mobile/src/components/gallery/GalleryDetailScreen.tsx"
      to: "apps/mobile/src/stores/galleryStore.ts"
      via: "toggleFavorite and likeMaterial actions"
      pattern: "toggleFavorite\\(|likeMaterial\\("
    - from: "apps/mobile/src/components/gallery/GalleryDetailScreen.tsx"
      to: "apps/mobile/src/stores/canvasStore.ts"
      via: "loadMaterialIntoCanvas action"
      pattern: "loadMaterialIntoCanvas\\(material\\)"
    - from: "apps/mobile/src/components/gallery/PublishModal.tsx"
      to: "apps/mobile/src/stores/galleryStore.ts"
      via: "publishMaterial action"
      pattern: "publishMaterial\\(\\)"
    - from: "apps/mobile/src/components/gallery/GalleryCard.tsx"
      to: "apps/mobile/src/components/gallery/GalleryDetailScreen.tsx"
      via: "Navigation from gallery to detail"
      pattern: "router.push.*gallery/detail.*id"
---

<objective>
Implement gallery interaction features: liking designs, favoriting for quick access, publishing own designs with metadata, and loading designs into canvas from gallery.

Purpose: Enable full community interaction with material gallery (like, favorite, publish, load).
Output: Working gallery detail screen with interaction buttons and publish modal.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-AI_素材广场与部署/06-02-PLAN.md (gallery store and Material interface)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From 06-02-PLAN.md (Material interface):
```typescript
interface Material {
  id: string;
  title: string;
  description: string;
  author: string;
  tags: string[];
  gridWidth: number;
  gridHeight: number;
  gridSize: number;
  pixelStyle: string;
  grid: ColorHex[][];
  createdAt: number;
  views: number;
  likes: number;
}
```

From 06-02-PLAN.md (existing gallery store):
```typescript
// Already has:
- materials: Material[]
- isLoading: boolean
- isOffline: boolean
- favorites: string[]
// Actions:
- fetchMaterials()
- setSearchQuery()
- publishMaterial()
// Need to add:
- toggleFavorite(id)
- isFavorite(id)
- likeMaterial(id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add like and favorite actions to gallery store</name>
  <files>apps/mobile/src/stores/galleryStore.ts</files>
  <action>Enhance gallery store with like and favorite functionality:

1. Add MMKV persistence for favorites:
   - Import MMKV from react-native-mmkv
   - Import persist middleware from zustand/middleware
   - Create storage instance: const storage = new MMKV()
   - Update persist config: persist({ name: 'gallery-storage', storage: { ...MMKVStorageAdapter ... } })
   - Ensure favorites array persists across app restarts

2. Add likeMaterial(id: string) action:
   - Find material by id in materials array
   - Increment material.likes by 1 (local optimistic update)
   - Note: This is local-only. Web app API has like endpoint, but we'll implement as local marking for simplicity
   - Could optionally call API to sync likes (deferred to future)

3. Add toggleFavorite(id: string) action:
   - Check if id exists in favorites array
   - If yes: remove from favorites (filter out id)
   - If no: add to favorites ( [...favorites, id] )
   - Optional haptic feedback (expo-haptics) on toggle

4. Update isFavorite(id: string) action:
   - Return favorites.includes(id)

5. Add getFavorites() action:
   - Return materials array filtered by favorites (all materials where id in favorites)
   - For favorites view (optional feature)

6. Add clearFavorites() action:
   - Clear favorites array (for reset/testing)

Export all new actions.

Note: Likes are local-only updates (not synced to server). Favorites are persisted in MMKV.
</action>
  <verify>
    <automated>grep -n "toggleFavorite\|likeMaterial\|isFavorite" apps/mobile/src/stores/galleryStore.ts</automated>
  </verify>
  <done>Gallery store supports liking and favoriting with MMKV persistence for favorites</done>
</task>

<task type="auto">
  <name>Task 2: Create gallery detail screen</name>
  <files>apps/mobile/src/components/gallery/GalleryDetailScreen.tsx</files>
  <action>Create detail screen for viewing and interacting with single design:

1. Import:
   - useState, useEffect from react
   - View, Text, ScrollView, TouchableOpacity, StyleSheet from react-native
   - MaterialIcons from @expo/vector-icons
   - { useLocalSearchParams, useRouter } from expo-router
   - { useGalleryStore } from stores/galleryStore
   - { useCanvasStore } from stores/canvasStore
   - Material interface from services/galleryService

2. Component state:
   - material: Material | null
   - isLiked (boolean, local state)
   - isFavorite (boolean, derived from store)

3. Get material by ID:
   - const { id } = useLocalSearchParams<{ id: string }>()
   - Find material in galleryStore.materials array by id
   - If not found, show "Design not found" message

4. UI layout (ScrollView):
   - Header: Back button + Title
   - Preview section: Full-size CanvasGrid showing material.grid
   - Info section: Title, Author, Description, Tags
   - Stats section: Views, Likes (with icons)
   - Actions section: Load into Canvas button, Favorite button, Like button

5. Preview:
   - Use CanvasGrid component (reuse from editor)
   - Display full grid: width=material.gridWidth, height=material.gridHeight
   - Show pixel style: material.pixelStyle (CIRCLE/SQUARE/ROUNDED)
   - Center preview with zoom/pan if possible (reuse existing canvas interactions)

6. Info display:
   - Title: Large, bold text
   - Author: Gray text "by {material.author}"
   - Description: Multiline text if material.description
   - Tags: Horizontal scroll of tag chips (rounded rectangles with text)

7. Stats display:
   - Views: MaterialIcons.visibility + material.views
   - Likes: MaterialIcons.favorite + material.likes (filled if isLiked, outlined if not)

8. Action buttons:
   - Load into Canvas: Large primary button, "Load into Canvas"
     - On press: useCanvasStore.loadMaterialIntoCanvas(material), navigate back to editor
   - Favorite: Button with heart icon (filled/outline based on isFavorite)
     - On press: galleryStore.toggleFavorite(material.id)
     - Haptic feedback (expo-haptics.impactAsync())
   - Like: Button with thumb up icon
     - On press: set isLiked(true), galleryStore.likeMaterial(material.id)
     - Haptic feedback

9. Accessibility:
   - accessibilityLabel for all buttons
   - accessibilityRole="button"
   - accessibilityHint: "Double tap to load design into canvas"

10. Styling:
    - Consistent spacing and padding
    - Theme colors from theme.ts
    - Responsive design

11. Add route: apps/mobile/app/(tabs)/gallery/[id].tsx
    - Import GalleryDetailScreen from components/gallery/GalleryDetailScreen
    - Render: <GalleryDetailScreen />
</action>
  <verify>
    <automated>grep -n "loadMaterialIntoCanvas\|toggleFavorite\|likeMaterial" apps/mobile/src/components/gallery/GalleryDetailScreen.tsx</automated>
  </verify>
  <done>Gallery detail screen shows full design with like, favorite, and load buttons</done>
</task>

<task type="auto">
  <name>Task 3: Add loadMaterialIntoCanvas action to canvas store</name>
  <files>apps/mobile/src/stores/canvasStore.ts</files>
  <action>Add action to load material from gallery into canvas:

1. Import Material interface from services/galleryService (or define inline if needed)

2. Add loadMaterialIntoCanvas(material: Material) action:
   - Parse material.grid as 2D array (already in correct format)
   - Set canvasGrid to material.grid
   - Set canvasWidth to material.gridWidth
   - Set canvasHeight to material.gridHeight
   - Set pixelStyle to material.pixelStyle (convert string to PixelStyle enum if needed)
   - Reset undo history (clear stack, clear redo stack)
   - Update canvas name to material.title (optional)

3. Ensure types match:
   - Material.grid is ColorHex[][] (same as canvasGrid type)
   - Material.pixelStyle is string (need to convert to PixelStyle enum)
   - Convert: PixelStyle[material.pixelStyle.toUpperCase() as keyof typeof PixelStyle]

4. Add action to canvasStore interface and implementation

Export loadMaterialIntoCanvas.
</action>
  <verify>
    <automated>grep -n "loadMaterialIntoCanvas" apps/mobile/src/stores/canvasStore.ts</automated>
  </verify>
  <done>canvasStore.loadMaterialIntoCanvas loads material grid into canvas and resets dimensions</done>
</task>

<task type="auto">
  <name>Task 4: Update GalleryCard to navigate to detail screen</name>
  <files>apps/mobile/src/components/gallery/GalleryCard.tsx</files>
  <action>Update GalleryCard to navigate to detail screen on press:

1. Import:
   - { useRouter } from expo-router
   - { useGalleryStore } from stores/galleryStore (for isFavorite check)

2. In component:
   - const router = useRouter()
   - const { isFavorite } = useGalleryStore()
   - const isFav = isFavorite(material.id)

3. Update onPress handler:
   - Navigate to detail screen: router.push(`/gallery/${material.id}`)
   - Use existing onPress prop or override

4. Update UI:
   - Add favorite indicator on card (small heart icon in corner)
   - Filled heart if isFav, outlined if not
   - Semi-transparent overlay for favorite indicator

5. Accessibility:
   - Update accessibilityLabel: `${material.title} by ${material.author}, ${isFav ? 'favorited' : 'not favorited'}`
   - accessibilityHint: "Double tap to view details"

6. Touch feedback:
   - Keep activeOpacity for TouchableOpacity
   - Optional haptic feedback on press (expo-haptics)

Update existing GalleryCard from plan 02.
</action>
  <verify>
    <automated>grep -n "router.push.*gallery/\|isFavorite\|isFav" apps/mobile/src/components/gallery/GalleryCard.tsx</automated>
  </verify>
  <done>GalleryCard navigates to detail screen and shows favorite indicator</done>
</task>

<task type="auto">
  <name>Task 5: Create publish modal for sharing designs</name>
  <files>apps/mobile/src/components/gallery/PublishModal.tsx</files>
  <action>Create modal for publishing current design to material gallery:

1. Import:
   - useState from react
   - View, Text, TextInput, TouchableOpacity, ScrollView, StyleSheet, Modal from react-native
   - MaterialIcons from @expo/vector-icons
   - { useCanvasStore } from stores/canvasStore
   - { useGalleryStore } from stores/galleryStore
   - { useProjectStore } from stores/projectStore (for project name)
   - Alert from react-native

2. Component state:
   - title (string)
   - author (string)
   - description (string)
   - tags (string, comma-separated)
   - isPublishing (boolean)
   - error (string | null)

3. Initialize from canvas/project:
   - On modal open: set title to projectStore.currentProject.name or "Untitled"
   - Set author to empty (user must enter)
   - Set description to empty
   - Set tags to empty

4. UI layout (ScrollView):
   - Header: "Publish Design" with close button
   - Title input: TextInput, placeholder "Design title", required
   - Author input: TextInput, placeholder "Your name", required
   - Description input: multiline TextInput, placeholder "Describe your design...", optional
   - Tags input: TextInput, placeholder "Tags (comma-separated)", optional
   - Publish button: disabled if publishing or missing required fields, "Publish" / "Publishing..."

5. Form validation:
   - Required fields: title, author
   - Optional: description, tags
   - Disable publish button if title.trim() === '' or author.trim() === ''

6. Publish logic:
   - Get canvas data from useCanvasStore: canvasGrid, canvasWidth, canvasHeight, pixelStyle
   - Parse tags: split by comma, trim whitespace, filter empty strings
   - Create data object: { title, author, description, tags, gridWidth: canvasWidth, gridHeight: canvasHeight, pixelStyle: pixelStyle.toString(), grid: canvasGrid }
   - Call galleryStore.publishMaterial(data)
   - On success: Alert.alert("Success", "Design published to gallery!"), close modal
   - On error: Alert.alert("Error", error.message), set isPublishing false

7. Accessibility:
   - accessibilityLabel for all inputs and buttons
   - accessibilityRole for interactive elements
   - accessibilityHint for required fields

8. Styling:
   - Slide-up modal animation (standard pattern)
   - Consistent spacing and padding
   - Theme colors from theme.ts

Modal slides up from bottom.
</action>
  <verify>
    <automated>grep -n "publishMaterial\|canvasGrid\|canvasWidth\|canvasHeight" apps/mobile/src/components/gallery/PublishModal.tsx</automated>
  </verify>
  <done>Publish modal collects metadata (title, author, description, tags) and publishes to gallery</done>
</task>

<task type="auto">
  <name>Task 6: Integrate publish modal into EditorScreen</name>
  <files>apps/mobile/app/(tabs)/editor.tsx</files>
  <action>Add publish button to editor screen for publishing current design:

1. Import:
   - PublishModal from components/gallery/PublishModal
   - useState from react

2. Add state:
   - showPublishModal (boolean)

3. Add publish button to tool drawer or toolbar:
   - Add to toolbar alongside AI Generate, AI Settings, Export buttons
   - Button: 44x44px, MaterialIcons.share icon or MaterialIcons.cloud_upload icon
   - Position: below export button or in tool drawer
   - Accessibility labels: "Publish to gallery", "Share your design with the community"

4. Render modal:
   - {showPublishModal && <PublishModal visible={showPublishModal} onClose={() => setShowPublishModal(false)} />}

5. Position buttons:
   - Add Publish button to toolbar row
   - Maintain consistent spacing (44x44px buttons)

6. Accessibility:
   - accessibilityLabel: "Publish to gallery"
   - accessibilityRole: "button"
   - accessibilityHint: "Double tap to publish current design"

7. Touch feedback:
   - Haptic feedback on button press (expo-haptics)

Update existing editor.tsx from plan 01 and 05.
</action>
  <verify>
    <automated>grep -n "PublishModal\|showPublishModal\|Publish" apps/mobile/app/(tabs)/editor.tsx</automated>
  </verify>
  <done>Publish button in editor opens PublishModal for sharing design to gallery</done>
</task>

<task type="auto">
  <name>Task 7: Add favorites view to gallery (optional)</name>
  <files>apps/mobile/src/components/gallery/GalleryFavoritesScreen.tsx</files>
  <action>Create favorites view for quick access to favorited designs (optional enhancement):

1. Import:
   - View, FlatList, StyleSheet from react-native
   - { useGalleryStore } from stores/galleryStore
   - GalleryCard from components/gallery/GalleryCard

2. UI layout:
   - Header: "My Favorites" title
   - FlatList with favorite materials

3. Get favorites:
   - const { getFavorites } = useGalleryStore()
   - const favorites = getFavorites()

4. FlatList configuration:
   - data: favorites
   - renderItem: GalleryCard (reuse component)
   - keyExtractor: material.id
   - numColumns: 2
   - columnWrapperStyle: row
   - Empty state: "No favorites yet" message

5. Add route: apps/mobile/app/(tabs)/gallery/favorites.tsx
   - Import GalleryFavoritesScreen from components/gallery/GalleryFavoritesScreen
   - Render: <GalleryFavoritesScreen />

6. Update gallery screen (06-02) to add favorites button:
   - Add button to gallery header: "Favorites" icon
   - Navigate to /gallery/favorites on press

This is optional but adds value for quick access to favorited designs.
</action>
  <verify>
    <automated>grep -n "getFavorites\|GalleryFavoritesScreen" apps/mobile/src/components/gallery/GalleryFavoritesScreen.tsx apps/mobile/src/components/gallery/GalleryScreen.tsx</automated>
  </verify>
  <done>Favorites view displays list of favorited designs (optional enhancement)</done>
</task>

</tasks>

<verification>
- Gallery detail screen shows full design with preview, info, stats, and actions
- Like button increments like count locally (optimistic update)
- Favorite button toggles favorite status with MMKV persistence
- Favorite indicator shows on gallery cards (filled heart icon)
- Load into Canvas button loads material grid into canvas and navigates to editor
- Publish modal collects metadata (title, author, description, tags) and publishes to gallery
- Publish button accessible from editor toolbar
- TypeScript compilation passes with 0 errors
</verification>

<success_criteria>
1. User can tap gallery card, view detail screen, and see full design preview
2. User can tap like button and see like count increment
3. User can tap favorite button and see favorite status toggle (persisted across restarts)
4. User can tap Load into Canvas and design loads into canvas
5. User can tap Publish button, enter metadata, and publish design to gallery
6. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-AI_素材广场与部署/06-03-SUMMARY.md`
</output>
