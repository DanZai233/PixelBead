---
phase: 05-数据管理与导出
plan: 03
type: execute
wave: 3
depends_on: [05-01]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/src/stores/canvasStore.ts
  - apps/mobile/src/components/OfflineIndicator.tsx
  - apps/mobile/src/components/OnboardingGuide.tsx
  - apps/mobile/src/screens/EditorScreen.tsx
  - apps/mobile/src/components/ToolDrawer.tsx
  - apps/mobile/app.json
autonomous: true
requirements: [PROJ-05, PROJ-06, OFFL-03, STAT-04, NAV-05, ACCS-01, ACCS-02, PERF-03]
user_setup: []
must_haves:
  truths:
    - User can undo actions up to 50 steps
    - User can redo undone actions
    - Offline status indicator shows when no internet connection
    - Onboarding guide shown on first app launch
    - Screen reader support works for all interactive elements
    - Dynamic text sizes supported via system settings
    - App startup time under 3 seconds
    - Undo/redo history persists within session
  artifacts:
    - path: "apps/mobile/src/stores/canvasStore.ts"
      provides: "Updated undo/redo with 50-step limit"
      min_lines: 250
    - path: "apps/mobile/src/components/OfflineIndicator.tsx"
      provides: "Offline status indicator with NetInfo"
      min_lines: 50
    - path: "apps/mobile/src/components/OnboardingGuide.tsx"
      provides: "First-time user onboarding with persistent dismissal"
      min_lines: 150
    - path: "apps/mobile/src/stores/onboardingStore.ts"
      provides: "Onboarding state with MMKV persistence"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/stores/canvasStore.ts"
      to: "pushToUndoStack"
      via: "Increase undo stack limit from 20 to 50"
      pattern: "slice.*0.*50"
    - from: "apps/mobile/src/components/OfflineIndicator.tsx"
      to: "@react-native-community/netinfo"
      via: "Listen to network state changes"
      pattern: "NetInfo.*fetch.*then"
    - from: "apps/mobile/src/components/OnboardingGuide.tsx"
      to: "onboardingStore"
      via: "Check if onboarding completed"
      pattern: "useOnboardingStore.*hasSeenOnboarding"
    - from: "apps/mobile/src/screens/EditorScreen.tsx"
      to: "OfflineIndicator"
      via: "Render indicator at top of screen"
      pattern: "OfflineIndicator.*style"
---

<objective>
Enhance undo/redo, add offline indicator, create onboarding guide, improve accessibility, and optimize startup performance

Purpose: These polish features complete the user experience: robust undo/redo, offline feedback, onboarding for new users, accessibility compliance, and fast startup.

Output: 50-step undo/redo, offline status indicator, dismissible onboarding guide, screen reader support, dynamic text scaling, and < 3s startup time.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-数据管理与导出/05-01-SUMMARY.md

# Existing codebase patterns
From apps/mobile/src/stores/canvasStore.ts (current undo/redo):
```typescript
pushToUndoStack: (grid) => {
  const { undoStack } = get();
  // Limit undo stack to 20 items (PROJ-05 requires 50)
  const newUndoStack = [JSON.stringify(mapToJson(grid)), ...undoStack].slice(0, 20);
  set({ undoStack: newUndoStack, redoStack: [] });
},
```

From Phase 3 (Drawing tools):
- Tool buttons have 44x44px tap targets (accessibility compliance)
- All buttons have accessibilityLabels

From Phase 2 (Canvas performance):
- CanvasGrid uses FlatList with viewport culling
- CanvasGrid uses React.memo for performance

From Phase 1 (Foundation):
- MMKV used for synchronous storage
- Zustand for state management
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance undo/redo to 50 steps</name>
  <files>
    apps/mobile/src/stores/canvasStore.ts
  </files>
  <action>
    Update apps/mobile/src/stores/canvasStore.ts:

    1. Update pushToUndoStack action:
       - Change limit from 20 to 50
       - const newUndoStack = [JSON.stringify(mapToJson(grid)), ...undoStack].slice(0, 50);

    2. Verify undo and redo actions handle 50-step stack correctly
    3. Add comment: "PROJ-05: Support up to 50 undo steps"

    This is a simple one-line change to increase the stack limit.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    Undo stack limit increased to 50, undo/redo actions work correctly with larger stack
  </done>
</task>

<task type="auto">
  <name>Task 2: Create onboarding store for persistent state</name>
  <files>
    apps/mobile/src/stores/onboardingStore.ts
  </files>
  <action>
    Create apps/mobile/src/stores/onboardingStore.ts:

    1. Import: create, persist, createJSONStorage from zustand/middleware
    2. Import: mmkvStorage from '../storage/mmkvStorage'

    3. Define OnboardingState interface:
       - hasSeenOnboarding: boolean
       - onboardingStep: number (0-5 for multi-step guide)

    4. Create useOnboardingStore with actions:
       - completeOnboarding: () => void (sets hasSeenOnboarding = true)
       - resetOnboarding: () => void (sets hasSeenOnboarding = false, onboardingStep = 0)
       - setOnboardingStep: (step: number) => void
       - nextStep: () => void (increments onboardingStep)
       - previousStep: () => void (decrements onboardingStep)

    5. Persist with name: 'onboarding-storage' using mmkvStorage
    6. Initialize: hasSeenOnboarding: false, onboardingStep: 0

    Pattern: Match other store files (canvasStore, colorStore, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    Onboarding store compiles, persists to MMKV, all actions defined
  </done>
</task>

<task type="auto">
  <name>Task 3: Build OnboardingGuide component</name>
  <files>
    apps/mobile/src/components/OnboardingGuide.tsx
  </files>
  <action>
    Create apps/mobile/src/components/OnboardingGuide.tsx:

    1. Import: React, { useState } from 'react'
    2. Import: { View, Text, TouchableOpacity, Image, StyleSheet, Dimensions } from 'react-native'
    3. Import: MaterialIcons from '@expo/vector-icons'
    4. Import: useOnboardingStore
    5. Component props: { visible: boolean; onDismiss: () => void }

    6. Define onboarding steps (array of objects):
       - Step 1: Welcome + app overview ("Welcome to PixelBead Studio!")
       - Step 2: Canvas basics ("Tap to place pixels, zoom to see details")
       - Step 3: Tools overview ("Use the tool drawer to switch between brush, eraser, fill, and more")
       - Step 4: Colors ("Select colors from the palette or use the HSL picker")
       - Step 5: Save & Export ("Save your projects and export as PNG")

    7. Component structure:
       - Full-screen modal with gradient background (purple to blue)
       - Left side: Illustration/image for current step (use placeholder or SVG icons)
       - Right side: Title, description, step indicator (dots)
       - Bottom: Navigation buttons (Skip, Previous, Next, Get Started)
       - Swipe gestures: Swipe left/right to navigate steps

    8. Step indicators:
       - Row of dots (5 dots total)
       - Current step: Blue, larger
       - Completed steps: Gray
       - Upcoming steps: Light gray, smaller

    9. Button logic:
       - Step 0: Only "Next" and "Skip" buttons
       - Steps 1-3: "Previous" + "Next" buttons
       - Step 4: "Previous" + "Get Started" buttons
       - On "Skip"/"Get Started": Call onboardingStore.completeOnboarding(), onDismiss()
       - On "Previous": onboardingStore.previousStep()
       - On "Next": onboardingStore.nextStep()

    10. Accessibility:
        - accessibilityLabel="Onboarding guide step {step + 1} of 5"
        - accessibilityRole="alert"
        - Announce step changes to screen readers
        - All buttons have accessibilityLabels

    11. Responsive design:
        - Portrait: Stack illustrations on top, text below
        - Landscape: Side-by-side layout
        - Use Dimensions.get('window') to detect orientation

    12. Styling: Beautiful gradients, smooth animations (use Animated API for slide transitions), clear typography

    Design: Modern, welcoming onboarding with clear steps and smooth animations.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    OnboardingGuide renders all 5 steps, navigation works, swipe gestures work, completion persists to MMKV, accessibility announcements work
  </done>
</task>

<task type="auto">
  <name>Task 4: Build OfflineIndicator component</name>
  <files>
    apps/mobile/src/components/OfflineIndicator.tsx
  </files>
  <action>
    Create apps/mobile/src/components/OfflineIndicator.tsx:

    1. Import: React, { useState, useEffect } from 'react'
    2. Import: { View, Text, StyleSheet } from 'react-native'
    3. Import: NetInfo from '@react-native-community/netinfo'
    4. Import: MaterialIcons from '@expo/vector-icons'
    5. Component props: none

    6. Component structure:
       - Conditionally render (only show when offline)
       - Banner at top of screen (full width, height: 40px)
       - Yellow/orange background (#FFC107) with icon + text
       - Icon: wifi-off (MaterialIcons)
       - Text: "You're offline" or "No internet connection"
       - Slide-in animation (use Animated API) when going from online to offline
       - Fade-out animation when going back online

    7. Network detection:
       - useEffect with NetInfo.addEventListener
       - Update local state: isOffline boolean
       - Show banner only when isOffline === true

    8. Accessibility:
       - accessibilityLabel="You are currently offline"
       - accessibilityRole="alert"
       - accessibilityLiveRegion="assertive" (announces to screen readers)
       - accessibilityHint="Core editing features still work offline"

    9. Styling: Consistent with app theme, subtle but visible, non-intrusive

    Design: Small banner at top of screen, dismissible via swipe down or auto-hide after 5 seconds (optional).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    OfflineIndicator shows when offline, hides when online, network state changes detected correctly, screen reader announcements work
  </done>
</task>

<task type="auto">
  <name>Task 5: Add screen reader support to all interactive elements</name>
  <files>
    apps/mobile/src/components/ToolDrawer.tsx
    apps/mobile/src/screens/EditorScreen.tsx
    apps/mobile/src/components/canvas/CanvasGrid.tsx
  </files>
  <action>
    Update accessibility properties in existing components:

    1. Review ToolDrawer.tsx:
       - Ensure all tool buttons have accessibilityLabel (e.g., "Brush tool", "Eraser tool")
       - Add accessibilityRole="button"
       - Add accessibilityState={{ selected: currentTool === tool }}
       - Add accessibilityHint="Double tap to select tool"

    2. Review CanvasGrid.tsx:
       - Add accessibilityLabel="Canvas grid, {gridSize.width} by {gridSize.height} pixels"
       - Add accessibilityRole="grid"
       - Ensure pixel cells don't block screen reader navigation (use accessible={false} on decorative elements)
       - Add announcement for color selection: accessibilityLabel="Selected color: {selectedColor}, use HSL to adjust"

    3. Review EditorScreen.tsx:
       - Ensure all action buttons have accessibilityLabels
       - Add accessibilityHint where actions aren't obvious
       - Use accessibilityLiveRegion="polite" for status updates (e.g., "Project saved")

    4. Add announcements for key actions:
       - Undo/redo: accessibilityLabel="Undo last action" / "Redo action"
       - Save: accessibilityLabel="Save project" + toast "Project saved" (use expo-av or Alert for announcements)
       - Export: accessibilityLabel="Export image"

    5. Test with VoiceOver (iOS) and TalkBack (Android) if possible during development

    Accessibility guidelines:
    - Use descriptive labels, not generic "Button 1"
    - Announce state changes (e.g., "Brush tool selected")
    - Use proper accessibilityRole (button, grid, switch, etc.)
    - Minimum tap targets: 44x44px (already met from Phase 3)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    All interactive elements have accessibilityLabels, accessibilityRoles set correctly, screen reader can navigate app, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 6: Add dynamic text size support</name>
  <files>
    apps/mobile/src/utils/theme.ts
  </files>
  <action>
    Create or update apps/mobile/src/utils/theme.ts:

    1. Import: { PixelRatio, Platform, Dimensions } from 'react-native'

    2. Create scaleFont utility function:
       - export function scaleFontSize(baseSize: number): number
       - Get user's font scale: PixelRatio.getFontScale() (iOS) or Platform.select({ android: ... })
       - Return: baseSize * fontScale
       - Clamp to reasonable range (0.8x to 1.5x) to prevent extreme scaling

    3. Create responsive font sizes object:
       - export const fontSizes = {
           xs: scaleFontSize(10),
           sm: scaleFontSize(12),
           base: scaleFontSize(14),
           md: scaleFontSize(16),
           lg: scaleFontSize(18),
           xl: scaleFontSize(20),
           xxl: scaleFontSize(24),
           xxxl: scaleFontSize(32),
         }

    4. Update existing components to use fontSizes instead of hardcoded values:
       - Replace fontSize: 14 with fontSize: fontSizes.base
       - Replace fontSize: 16 with fontSize: fontSizes.md
       - Apply to ToolDrawer, ProjectList, OnboardingGuide, etc.

    5. If theme.ts doesn't exist, create it with:
       - Font sizes (dynamic)
       - Colors (reuse from existing theme if exists)
       - Spacing constants

    This ensures text scales according to user's system accessibility settings.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    theme.ts created with dynamic font sizes, components use fontSizes instead of hardcoded values, text scales with system settings
  </done>
</task>

<task type="auto">
  <name>Task 7: Optimize app startup performance</name>
  <files>
    apps/mobile/app.json
    apps/mobile/src/App.tsx
    apps/mobile/src/stores/canvasStore.ts
  </files>
  <action>
    Optimize app startup to achieve < 3 seconds:

    1. Review app.json:
       - Ensure "splash" is configured with local image (no network fetch)
       - Set "backgroundColor" to match splash screen for smooth transition
       - Remove unnecessary plugins from Expo plugins list

    2. Review App.tsx:
       - Use React.lazy() for lazy loading heavy components:
         - const EditorScreen = React.lazy(() => import('./screens/EditorScreen'))
         - const GalleryScreen = React.lazy(() => import('./screens/GalleryScreen'))
       - Wrap with Suspense:
         - <Suspense fallback={<LoadingSpinner />}>
       - Move initialization code to useEffect (not top-level render)
       - Use InteractionManager.runAfterInteractions() for non-critical setup

    3. Review store initialization:
       - Ensure MMKV storage is initialized synchronously
       - Avoid async operations in store initialization
       - Consider loading large data (palettes) asynchronously with progress indicator

    4. Code splitting:
       - Identify heavy components (color picker, export modal)
       - Move to separate files, load on demand

    5. Performance measurement:
       - Add console.time('app-startup') at app entry
       - Add console.timeEnd('app-startup') after all stores hydrated
       - Remove in production (already handled by build pipeline from Phase 1)

    6. Use Hermes engine:
       - Verify app.json has "jsEngine": "hermes"
       - Hermes is enabled by default in Expo SDK 55

    7. Profile with Flipper:
       - Identify slow initial renders
       - Optimize with React.memo, useMemo, useCallback

    Goal: App loads and renders initial screen in < 3 seconds on mid-range device.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    App startup time measured and optimized, React.lazy applied to heavy components, MMKV initialization is synchronous, all async operations deferred
  </done>
</task>

<task type="auto">
  <name>Task 8: Integrate onboarding and offline indicator into EditorScreen</name>
  <files>
    apps/mobile/src/screens/EditorScreen.tsx
  </files>
  <action>
    Update apps/mobile/src/screens/EditorScreen.tsx:

    1. Import: OnboardingGuide from '../components/OnboardingGuide'
    2. Import: OfflineIndicator from '../components/OfflineIndicator'
    3. Import: useOnboardingStore

    4. Add local state:
       - onboardingVisible: boolean

    5. useEffect for onboarding:
       - Check: !useOnboardingStore(state => state.hasSeenOnboarding)
       - If false: set onboardingVisible(true)

    6. Render OfflineIndicator:
       - <OfflineIndicator />
       - Position: At top of screen, above all other content
       - Use absolute positioning or View wrapper

    7. Render OnboardingGuide:
       - <OnboardingGuide
           visible={onboardingVisible}
           onDismiss={() => setOnboardingVisible(false)}
         />
       - Full-screen overlay (zIndex: 9999)

    8. Add to app entry (App.tsx):
       - Show onboarding on first app launch (not just EditorScreen)
       - Or keep in EditorScreen if onboarding is editor-focused

    Design: Onboarding shows on first launch only, offline indicator always available when offline.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    OnboardingGuide shows on first launch, can be dismissed, doesn't show again. OfflineIndicator shows when offline. Both integrate without breaking existing UI.
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **TypeScript compilation**: Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` — must pass with 0 errors
2. **Undo/redo limit**: Test that 50 actions can be undone, not 20
3. **Onboarding persistence**: Test that onboarding doesn't reappear after app restart
4. **Offline indicator**: Test that indicator appears/disappears with network state changes
5. **Screen reader**: Test navigation with VoiceOver (iOS) or TalkBack (Android)
6. **Dynamic text**: Test that text scales when changing system accessibility font size
7. **Startup performance**: Measure app startup time, must be < 3 seconds on mid-range device
8. **Integration**: Test that onboarding and offline indicator don't interfere with canvas interactions
</verification>

<success_criteria>
1. User can undo up to 50 actions (verified by counting undos)
2. User can redo undone actions (redo stack works correctly)
3. Offline indicator appears when network disconnected, hides when connected
4. Onboarding guide shows on first app launch, can be completed, doesn't reappear
5. All interactive elements are accessible via screen reader with proper labels and roles
6. Text scales according to system accessibility settings (small, medium, large)
7. App starts and renders initial screen in under 3 seconds
8. Undo/redo history persists within session (not across restarts, which is correct)
9. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-数据管理与导出/05-03-SUMMARY.md`
</output>
