---
phase: 05-数据管理与导出
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/src/stores/projectStore.ts
  - apps/mobile/src/components/project/ProjectList.tsx
  - apps/mobile/src/components/project/NewProjectModal.tsx
  - apps/mobile/src/components/project/ProjectManager.tsx
  - apps/mobile/src/utils/projectStorage.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-07, PROJ-08, STAT-02, OFFL-02]
user_setup: []
must_haves:
  truths:
    - User can save current project with custom name
    - User can load saved projects from local storage
    - User can export project as JSON file
    - User can import project from JSON file
    - User can create new canvas with custom size
    - User can clear current canvas with confirmation
    - Project operations work completely offline
    - Projects persist across app restarts
  artifacts:
    - path: "apps/mobile/src/stores/projectStore.ts"
      provides: "Project state management with MMKV persistence"
      exports: ["useProjectStore", "Project"]
      min_lines: 120
    - path: "apps/mobile/src/utils/projectStorage.ts"
      provides: "Project serialization/deserialization utilities"
      min_lines: 80
    - path: "apps/mobile/src/components/project/ProjectList.tsx"
      provides: "Project list component with metadata"
      min_lines: 100
    - path: "apps/mobile/src/components/project/NewProjectModal.tsx"
      provides: "New canvas creation modal"
      min_lines: 60
    - path: "apps/mobile/src/components/project/ProjectManager.tsx"
      provides: "Project management UI (save, load, import, export)"
      min_lines: 150
  key_links:
    - from: "apps/mobile/src/stores/projectStore.ts"
      to: "mmkvStorage"
      via: "Persist projects using MMKV storage"
      pattern: "persist.*mmkvStorage"
    - from: "apps/mobile/src/components/project/ProjectManager.tsx"
      to: "useProjectStore"
      via: "Subscribe to projects state and trigger actions"
      pattern: "useProjectStore\\(.*projects.*\\)"
    - from: "apps/mobile/src/utils/projectStorage.ts"
      to: "useCanvasStore"
      via: "Import/export canvas state"
      pattern: "import.*canvasStore"
    - from: "apps/mobile/src/stores/projectStore.ts"
      to: "FileSystem from expo-file-system"
      via: "Save/Load JSON files to device storage"
      pattern: "FileSystem.*readAsStringAsync"
---

<objective>
Deliver complete project management system with save, load, import, export, and canvas creation capabilities

Purpose: Users need to manage multiple projects, save their work, and export/import project files. This is fundamental for any creative tool.

Output: Working project management system with MMKV persistence, JSON import/export, and new canvas creation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-颜色与高级特性/04-01-SUMMARY.md

# Existing codebase patterns
From apps/mobile/src/stores/canvasStore.ts:
```typescript
// Helper to convert Map to/from JSON
function mapToJson(map: Map<string, string>): Record<string, string> {
  return Object.fromEntries(map.entries());
}

function jsonToMap(json: Record<string, string>): Map<string, string> {
  return new Map(Object.entries(json));
}

export const useCanvasStore = create<CanvasState>()(
  persist(
    (set, get) => ({
      grid: new Map(),
      gridSize: { width: 20, height: 20 },
      // ... other state
    }),
    { name: 'canvas-storage', storage: createJSONStorage(() => mmkvStorage) }
  )
);
```

From packages/shared-types/src/types.ts (shared):
```typescript
export type ColorHex = string; // Format: #RRGGBB
export type PixelStyle = 'CIRCLE' | 'SQUARE' | 'ROUNDED';
export type ToolType = 'brush' | 'eraser' | 'fill' | 'picker' | 'line' | 'rectangle' | 'circle';
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project storage utilities</name>
  <files>
    apps/mobile/src/utils/projectStorage.ts
  </files>
  <action>
    Create apps/mobile/src/utils/projectStorage.ts:

    1. Define TypeScript interfaces:
       - interface Project { id: string; name: string; gridSize: { width: number; height: number }; grid: Record<string, string>; createdAt: string; updatedAt: string; }
       - interface ProjectExport { name: string; gridSize: { width: number; height: number }; grid: Record<string, string>; version: string; }

    2. Create utility functions:
       - serializeProject(canvasStore: CanvasState, name: string): ProjectExport
         - Extract: gridSize, grid (convert Map to object), version: "1.0"
       - deserializeProject(data: ProjectExport): Partial<CanvasState>
         - Return: { gridSize, grid (convert object to Map) }
       - generateProjectId(): string (use timestamp + random)
       - validateProjectData(data: any): boolean (check required fields, grid size limits 4-200)

    3. Import types from @pixelbead/shared-types (ColorHex, PixelStyle)
    4. Add JSDoc comments for all functions

    This is pure utilities layer, no UI, no state management.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    projectStorage.ts exports 5 functions, all TypeScript types compile, serialize/deserialize round-trip works
  </done>
</task>

<task type="auto">
  <name>Task 2: Create project store with MMKV persistence</name>
  <files>
    apps/mobile/src/stores/projectStore.ts
  </files>
  <action>
    Create apps/mobile/src/stores/projectStore.ts:

    1. Import: create, persist, createJSONStorage from zustand/middleware
    2. Import: mmkvStorage from '../storage/mmkvStorage'
    3. Import: { serializeProject, deserializeProject, generateProjectId } from '../utils/projectStorage'
    4. Import: useCanvasStore

    5. Define ProjectState interface:
       - projects: Project[] (array of saved projects)
       - currentProjectId: string | null
       - isSaving: boolean
       - isLoading: boolean

    6. Create useProjectStore with actions:
       - saveProject(name: string): void
         - Generate project ID
         - Serialize from canvasStore using serializeProject
         - Create Project object with createdAt/updatedAt
         - Update or add to projects array
         - Set currentProjectId
       - loadProject(projectId: string): void
         - Find project in projects array
         - Deserialize project data using deserializeProject
         - Update canvasStore (gridSize, grid)
         - Set currentProjectId
         - Reset undoStack/redoStack
       - deleteProject(projectId: string): void
         - Remove from projects array
         - If currentProjectId == projectId, set currentProjectId = null
       - createNewCanvas(width: number, height: number): void
         - Clear canvasStore (new Map, new gridSize)
         - Reset undoStack/redoStack
         - Set currentProjectId = null
       - clearCurrentCanvas(): void
         - Call canvasStore.clearCanvas()
       - exportProjectAsJson(projectId: string): Promise<string> (return JSON string)
       - importProjectFromJson(jsonData: string): void
         - Parse JSON, validate using validateProjectData
         - Create new Project from imported data
         - Add to projects array, set as current

    7. Persist with name: 'project-storage' using mmkvStorage
    8. Initialize: projects: [], currentProjectId: null, isSaving: false, isLoading: false

    Pattern reference: Match canvasStore.ts structure for Zustand + MMKV persistence.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    Project store compiles, persists to MMKV, integrates with canvasStore, all actions defined
  </done>
</task>

<task type="auto">
  <name>Task 3: Build ProjectList component</name>
  <files>
    apps/mobile/src/components/project/ProjectList.tsx
  </files>
  <action>
    Create apps/mobile/src/components/project/ProjectList.tsx:

    1. Import: React, View, FlatList, TouchableOpacity, Text, StyleSheet from 'react-native'
    2. Import: useProjectStore, MaterialIcons from '@expo/vector-icons'
    3. Component props: { onSelectProject: (projectId: string) => void }

    4. Component structure:
       - FlatList rendering ProjectItem components
       - ProjectItem: Shows project name, size (e.g., "20x20"), date (formatted)
       - Delete button: trash icon, 44x44px tap target
       - Load button: chevron-right icon
       - Empty state: "No saved projects" message

    5. ProjectItem layout (horizontal row):
       - Left: Project icon + name + metadata
       - Right: Load + Delete buttons
       - Selected indicator: blue background if matches currentProjectId

    6. Date formatting: "2 hours ago", "Yesterday", "Mar 28" (use Intl.DateTimeFormat)

    7. Accessibility:
       - accessibilityLabel="{project.name}, {size} grid"
       - accessibilityRole="button"
       - accessibilityHint="Double tap to load project"

    8. Styling: Use StyleSheet, consistent with app theme

    Use FlatList for performance with many projects.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    ProjectList renders projects, tap loads project, delete removes project, empty state shows, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Build NewProjectModal component</name>
  <files>
    apps/mobile/src/components/project/NewProjectModal.tsx
  </files>
  <action>
    Create apps/mobile/src/components/project/NewProjectModal.tsx:

    1. Import: React, { useState } from 'react'
    2. Import: { Modal, View, Text, TextInput, TouchableOpacity, StyleSheet } from 'react-native'
    3. Import: MaterialIcons from '@expo/vector-icons'
    4. Component props: { visible: boolean; onClose: () => void; onCreate: (name: string, width: number, height: number) => void }

    5. Component structure:
       - Modal with transparent overlay (black with 50% opacity)
       - White card centered (width: 80%, maxWidth: 400px)
       - Input: Project name (required, max 50 chars)
       - Inputs: Width and Height (number, range 4-200, default 20)
       - Preset buttons: [8x8, 16x16, 20x20, 32x32, 48x48] for quick selection
       - Actions: Cancel (gray) + Create (blue)

    6. Validation:
       - Name: Required, no special chars
       - Size: 4-200 range validation
       - Create button disabled if invalid

    7. Accessibility:
       - accessibilityLabel="New project modal"
       - accessibilityRole="dialog"
       - Input accessibilityLabel="Project name", "Canvas width", "Canvas height"

    8. Styling: Modal animation (slide up from bottom using Animated.View)

    Design: Matches app modal style from other screens.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    NewProjectModal renders correctly, validation works, preset buttons update inputs, onCreate callback fires, no TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 5: Build ProjectManager component (save, load, import, export)</name>
  <files>
    apps/mobile/src/components/project/ProjectManager.tsx
  </files>
  <action>
    Create apps/mobile/src/components/project/ProjectManager.tsx:

    1. Import: React, { useState } from 'react'
    2. Import: { View, TouchableOpacity, Alert, StyleSheet } from 'react-native'
    3. Import: * as DocumentPicker from 'expo-document-picker'
    4. Import: * as FileSystem from 'expo-file-system'
    5. Import: * as Sharing from 'expo-sharing'
    6. Import: MaterialIcons from '@expo/vector-icons'
    7. Import: useProjectStore, useCanvasStore
    8. Import: ProjectList from './ProjectList'
    9. Import: NewProjectModal from './NewProjectModal'

    10. Component structure:
        - Horizontal toolbar with 4 buttons (Save, Load, Import, Export, New Canvas, Clear)
        - Save button: Call projectStore.saveProject(currentProjectName || "Untitled")
        - Load button: Show modal with ProjectList
        - Import button: Use DocumentPicker.pickAsync({ type: 'application/json' })
        - Export button: Call projectStore.exportProjectAsJson, save to file using FileSystem.writeAsStringAsync, call Sharing.shareAsync
        - New Canvas button: Show NewProjectModal
        - Clear button: Alert confirmation, call projectStore.clearCurrentCanvas

    11. Save flow:
        - If currentProjectId exists, update existing project
        - If new project, prompt for name (simple Alert.prompt)
        - On success: Show toast "Project saved!"

    12. Load flow:
        - Show modal with ProjectList
        - On project select: Call projectStore.loadProject(projectId), close modal

    13. Import flow:
        - DocumentPicker.pickAsync({ type: 'application/json' })
        - FileSystem.readAsStringAsync(file.uri)
        - projectStore.importProjectFromJson(jsonData)
        - Show toast "Project imported!"

    14. Export flow:
        - projectStore.exportProjectAsJson(currentProjectId)
        - Write to temporary file (FileSystem.documentDirectory + 'project_export.json')
        - Sharing.shareAsync(fileUri)
        - On success: Show toast "Project exported!"

    15. New Canvas flow:
        - Show NewProjectModal
        - On create: Call projectStore.createNewCanvas(width, height)

    16. Clear flow:
        - Alert.alert("Clear Canvas", "Are you sure? This cannot be undone.", [
            { text: "Cancel", style: "cancel" },
            { text: "Clear", style: "destructive", onPress: () => projectStore.clearCurrentCanvas() }
          ])

    17. Accessibility: All buttons have 44x44px tap targets, accessibilityLabels

    18. Styling: Icon buttons with labels below, consistent with tool drawer

    Design: Floating action button or toolbar in editor screen.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>
    ProjectManager renders all buttons, save/load/import/export/clear/new flows work, no TypeScript errors
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. **TypeScript compilation**: Run `npx tsc --noEmit --project apps/mobile/tsconfig.json` — must pass with 0 errors
2. **Project persistence**: Test that saved projects persist after app restart (MMKV storage)
3. **Project serialization**: Test that serialize/deserialize round-trip preserves all canvas state
4. **Import/export validation**: Test that invalid JSON files are rejected with error message
5. **Component rendering**: Verify all components render without crashes on iOS and Android
6. **Offline functionality**: Test that all project operations work with airplane mode enabled
</verification>

<success_criteria>
1. User can save current project with custom name, project appears in project list
2. User can load saved projects from project list, canvas state restored correctly
3. User can export project as JSON file, file can be shared via system share sheet
4. User can import project from JSON file, canvas state loaded correctly
5. User can create new canvas with custom size (4-200), canvas cleared and resized
6. User can clear current canvas with confirmation dialog, undo history reset
7. All project operations work completely offline (no network required)
8. Projects persist across app restarts via MMKV storage
9. TypeScript compilation passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-数据管理与导出/05-01-SUMMARY.md`
</output>
