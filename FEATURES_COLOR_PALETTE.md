# 拼豆色板、颜色合并与已有颜色

## 功能概览

拼豆糕手提供以下色板相关能力：

1. **多品牌色号系统**：MARD、COCO、漫漫、盼盼、咪小窝。
2. **多档色板预设**：全色板、168 色、144 色、96 色、48 色和自定义。
3. **K-Means 相似颜色合并**：把画布颜色归并到指定目标颜色数。
4. **颜色映射**：把画布颜色替换为品牌色板中最接近的色号。
5. **已有颜色优先映射**：优先使用用户手头已有的拼豆颜色。
6. **严格模式**：只允许映射到已有颜色，不引入新色号。
7. **大屏展开色板**：按色系浏览、勾选和统计已有颜色。

## 核心概念

### 色号系统

色号系统决定界面和导出图纸中显示哪个品牌的色号。当前支持：

- MARD
- COCO
- 漫漫
- 盼盼
- 咪小窝

选择色号系统不会直接修改画布，只有执行「映射到色板」后才会替换颜色。

### 色板预设

色板预设用于限制映射时最多使用的颜色数量：

- **全色板**：不限制颜色数量。
- **168 / 144 / 96 / 48 色**：映射到对应规模的色板。
- **自定义**：由用户结合目标颜色数、合并和已有颜色自行控制。

切换到有限颜色数的预设时，应用会询问是否立即执行映射。

### 目标颜色数

「目标颜色数」用于 K-Means 颜色合并，可选择 4、8、12、16、24 或 32 色。

它表示“希望画布最终压缩到多少种颜色”，不是直接把每种颜色替换成品牌色号。需要实际色号时，仍应执行一次「映射到色板」。

### 合并相似颜色与映射到色板的区别

| 操作 | 作用 | 适用场景 |
| --- | --- | --- |
| 合并相似颜色 | 使用 K-Means 将相近颜色归并到目标颜色数 | 图片颜色过多，先压缩整体色数 |
| 映射到色板 | 把画布颜色替换为品牌色板中最接近的色号 | 准备购买拼豆，需要明确可购买色号 |

推荐先合并、再映射。

## 推荐工作流

### 图片或智能生成后的标准处理

1. 在左侧「色板设置」选择实际使用的色号系统。
2. 选择色板预设，或保持全色板。
3. 点「合并相似颜色」，把颜色减少到适合制作的目标数量。
4. 点「映射到色板」，把剩余颜色换成对应品牌色号。
5. 开启「显示色号」，检查颜色统计和导出图纸。

### 使用手头已有拼豆

1. 先选择你实际使用的色号系统。
2. 在「我的已有颜色」中逐一点选已有色块。
3. 也可以输入 `#RRGGBB` 自定义颜色，或点「从画布添加」加入当前作品使用的颜色。
4. 根据需要点击「展开色板」，按色系浏览和核对已选颜色。
5. 点「映射到色板」。相近颜色会优先使用已有颜色，明显不同的颜色会回退到完整色板。
6. 如果希望绝不引入新色号，开启「严格模式」后再次映射。

## 我的已有颜色

「我的已有颜色」会保存在浏览器本地，下次打开时自动恢复。

可用操作：

- **逐色选择**：点击色块加入或移除已有颜色。
- **HEX 添加**：输入 `#RRGGBB` 添加色板外的自定义颜色。
- **从画布添加**：把当前画布使用的所有非透明颜色加入已有颜色。
- **清空**：删除全部已有颜色记录。
- **展开色板**：按颜色系列查看色卡、已选数量和整体比例。
- **严格模式**：只映射到已有颜色，允许保留无法匹配的原始颜色，不自动引入新色。

## 显示与导出

开启「显示色号」后：

- 颜色统计显示品牌色号。
- 色块悬停时显示色号和 HEX。
- 导出图纸时显示对应色号。

如果只需要颜色统计而不需要色号标注，可以关闭该选项。

## 颜色处理实现

核心逻辑位于：

- `utils/colorSystemUtils.ts`：颜色距离、K-Means 聚类、色板映射、已有颜色优先映射。
- `hooks/useEditorPalette.ts`：色板状态、已有颜色持久化、合并与映射入口。
- `components/OwnedPaletteModal.tsx`：已有颜色大屏展开色板和统计。
- `colorSystemMapping.json`：多品牌色号映射数据。

当前「合并相似颜色」使用 K-Means 聚类，不再使用旧版的固定百分比合并阈值。

## 注意事项

1. 合并颜色和映射色板都会修改画布，可以使用 `Ctrl+Z` 撤销。
2. 不同品牌的色号体系可能不同，请选择和实际库存一致的品牌。
3. 普通模式会在已有颜色明显不匹配时回退到完整色板；严格模式不会引入新色号。
4. 图片转换和智能生成后建议检查一次色板映射，避免出现难以购买的近似色。
5. 切换色板预设时可能立即触发映射，重要作品建议先保存或导出 JSON。
