# Capacitor (iOS/Android) build environment
# Set VITE_API_BASE_URL to your Vercel deployment domain so /api/ routes resolve correctly.
VITE_API_BASE_URL=https://pindou.danzaii.cn

# 分享：原生 App 始终请求 VITE_API_BASE_URL/api/share（WebView 直连 Upstash 易被 CORS 拒绝）。
# 可选：仍可将 VITE_UPSTASH_* 打进包内（其他逻辑回退用）；分享在 iOS/Android 不依赖直连。
VITE_UPSTASH_REDIS_REST_URL=https://singular-shrimp-51727.upstash.io
VITE_UPSTASH_REDIS_REST_TOKEN=AcoPAAIncDIxYWM0MzczMGU1MmY0NGIzOTQxZjA0YjZlMzQwMmM4Y3AyNTE3Mjc